# Implementation status

Phase 2 — Conversation / Attention Engine, central context convergence and reference resolution (2026-09-03): implementato senza avviare Phase 3 e senza creare un secondo Cognitive Core, Context Engine, MemoryStore o voice stack. La fonte canonica è `CORE_RUNTIME.context`, con `CORE_RUNTIME.memory.working` per il breve termine volatile e `CORE_RUNTIME.context.operational` per il solo risultato operativo reale/verificato con TTL e verification gate. Aggiunto `jarvis_core/reference_resolution.py` con `ReferenceResolution` e `ReferenceResolver`: supporta application/window, artifact/result, topic/entity, ordinali e alternative, con confidence, source, TTL e clarification fail-closed. Working Memory usa chiavi `conversation.last_user_turn`, `conversation.last_assistant_turn`, `conversation.focus`, `conversation.entities`, `conversation.references`, `conversation.pending_proposal`, `conversation.last_action` e `conversation.active_object`; il `ContextEngine` canonico espone la vista strutturata in `snapshot()["conversation"]`, redatta e bounded. `context_engine.py` è ora facade deprecata lazy: delega al runtime canonico e non scrive più `jarvis_context.json`; il file legacy non viene cancellato. `main.py` risolve riferimenti applicativi e ambiguità prima del router; `brain.py` registra azioni verificate e usa il contesto canonico; AI riceve solo un compact current context; HUD usa il runtime canonico con fallback compatibile per fixture legacy. Operational follow-up, confirmation, permission e verification restano prioritari e invariati. Test mirati Phase 2: 65/65; regressione completa: 555/555 OK; compileall, Ruff e pip check superati. Checklist manuale: `tests/manual_phase2_conversation.py`, non eseguita automaticamente. Backup baseline: `backups/phase2-conversation-context-baseline_20260903.zip` SHA-256 `832fa85d13a5a9459fa24808b27e4b6fae4f1e3b4a2d290d98dd9e22a239f783`; backup finale: `backups/phase2-conversation-context-final_20260903.zip` SHA-256 `99a9d48e02871a9597dcf76945c3993919e894f01da41c6b735ca8dd711bc13c` (hash calcolato prima dell’aggiornamento di questa riga).

Phase 1 muted wake phrase correction (2026-09-03): corretto esclusivamente il recupero della frase completa quando JARVIS è in `MUTED`. `wakeword.aspetta_jarvis()` mantiene la grammatica primaria conservativa con sole varianti di Jarvis e `[unk]`, verifica prima speaker/wake come già previsto e poi usa una sola volta `StreamingTranscriber` locale unrestricted sui blocchi PCM recenti già acquisiti. Se trova una variante valida salva temporaneamente wake+comando; in caso di errore conserva comunque `jarvis` e riattiva JARVIS. Nessun nuovo STT/modello/voice stack, nessuna chiamata OpenAI, nessun WAV o dato persistente. Il buffer `_recent_audio` è bounded a 40 blocchi × 2000 campioni / 16000 Hz = 5 secondi e viene svuotato dopo il recupero. `recupera_frase_wake()` cancella il valore volatile dopo la lettura, consentendo a `main.py` di passare `apri chrome` come richiesta iniziale e aprire la conversazione. Test mirati wake/attention/voice: 24/24; regressione completa: 547/547 OK; compileall, Ruff e pip check superati. Test hardware non eseguiti. Backup baseline: `backups/phase1-muted-wake-phrase-fix-baseline_20260903.zip`; backup finale: `backups/phase1-muted-wake-phrase-fix-final_20260903.zip`. Phase 2 non iniziata.

Selective attention integration correction — Phase 1 (2026-09-03): corrette esclusivamente le integrazioni della Phase 1 esistente. La configurazione standard caricata dal runtime mantiene `continuous_listening=true` e usa `wake_word_only_standby=false`, lasciando `true` come modalità legacy manuale. Il gate selective usa il PCM int16 volatile della frase appena catturata, convertito localmente in float32 normalizzato e verificato da `IdentityService.recognize_voice_samples()` con `voice_match_threshold`; `owner_speaker` è `True` solo per il profilo `ceo_profile_name`, `False` per un altro profilo riconosciuto e `None` quando la verifica non è disponibile/decisiva. La sessione permessi non viene usata come prova speaker. Wake word e prefisso vengono riusati tramite `interpreta_richiamo_jarvis()`: `Jarvis` apre la conversazione senza inviare la parola al router, mentre `Jarvis, apri Chrome` inoltra solo `apri chrome`; la frase completa viene mantenuta volatile anche nel percorso legacy da MUTED. Il selective path resta locale-only (`allow_cloud=False`), non usa OpenAI transcription e non scrive campioni ambientali su disco. Nessun activity detector nuovo è stato aggiunto: `activity_relevant=False` esplicito. Verifiche: test mirati 13/13; regressione completa 542/542; compileall, Ruff sui file modificati e pip check superati. Test hardware/manuali con microfono reale non eseguiti. Backup baseline: `backups/phase1-attention-integration-fix-baseline_20260903.zip`; backup finale: `backups/phase1-attention-integration-fix-final_20260903.zip`. Non è stata iniziata la Phase 2.

Selective attention / persistent intelligent mute — Phase 1 (2026-09-03): aggiunto `jarvis_voice.attention.AttentionController`, stato sociale condiviso tramite la chiave `conversation_attention` dello `StateManager` esistente e decisione conservative addressedness con evidenze esplicite, contesto, owner speaker, intento operativo e segnali negativi. La wake word resta il fallback a confidenza 1.0 e l'unico risveglio da `MUTED`; `MUTED` interrompe TTS, chiude la conversazione e scarta ogni parlato non-wake. Aggiunto `resolve_control_intent()` in `decision_layer.py` per famiglie semantiche di mute/stop, con protezione contestuale per "basta". Lo standby selettivo riusa VAD e streaming STT locale attraverso `voice.ascolta(..., allow_cloud=False)` e non invia frasi ambientali al cloud; con `wake_word_only_standby` resta attivo il percorso legacy.

File modificati/creati: `jarvis_voice/attention.py`, `decision_layer.py`, `voice.py`, `main.py`, `tests/test_voice_attention.py`, `tests/manual_phase1_attention.py`. Checkpoint baseline: `backups/phase1-attention-baseline_20260903.zip`, SHA-256 `468B42802B4C026E4FA2C4419DADF47C779DC19159B8EE888905FAFB950CFAC7`. Test mirati: 71/71 `OK` (attention, decision layer, streaming STT, transcript repair, VAD, wake word, voice session, barge-in, speaker identity, StateManager/StateMachine). Regressione completa: 537/537 `OK`; `compileall`: superato; Ruff sui file modificati: superato; `pip check`: `No broken requirements found`. Test hardware/manuali con microfono reale non eseguiti; il checklist è in `tests/manual_phase1_attention.py` e non viene dichiarato superato. Checkpoint finale: `backups/phase1-attention-final_20260903.zip`, SHA-256 `AD599446FFF2A790DCB67575F340EC9E9AE71586320C89A4B6C93EA1813A9642`.

Chrome control diagnostic final (2026-09-02): corretto `tools._avvia_chrome_controllato()` per distinguere il launch OS di Chrome gia attivo dal profilo JARVIS controllato; il router `Apri Chrome` resta `apri_programma`, mentre tab/navigazione usano bridge, CDP e fallback visuale gia esistenti. Verifiche mirate Chrome/browser: 62/62; regressione completa: 513/513 `OK`; compileall e `pip check` superati. E2E GUI/main reale: avvio dal launcher e chiusura WM_CLOSE verificati; sessione controllata ha esposto CDP 9222, aperto YouTube e prodotto snapshot YouTube; apertura con Chrome gia attivo ha usato il launch OS. Pronuncia vocale umana e interazione YouTube completa non sono state eseguite automaticamente nell'ambiente disponibile e non vengono dichiarate superate. Checkpoint finale: `backups/chrome-control-final_20260902_181601.zip`, SHA-256 `6A8EB706A351835BF1BA582936D5F6FEBCEA1FAF3A5C02BD3F074920BF189BFD`.

Chrome control diagnostic baseline (2026-09-02): checkpoint recuperabile `backups/chrome-control-baseline_20260902_175322.zip`, SHA-256 `271CA1FF651355203E959F4CE2F9CF22464012BB4DC3E2AAA8FCDA73995DA67E`. Tracciati router locale, launcher OS, bridge MV3, fallback CDP/visual e percorso GUI/main; il checkpoint e stato creato prima della correzione.

Silero VAD integration final (2026-09-02): aggiunto provider lazy condiviso CPU-only in `jarvis_voice/vad.py`, integrato nel punto centrale `voice.ascolta()` come decisione ibrida WebRTC/RMS + Silero. Configurazione: provider `silero`, threshold `0.55`, release `0.35`, min speech `180 ms`, min silence `540 ms`, grace `300 ms`, pre-roll `300 ms`; `silero_enabled=false` ripristina il percorso legacy. Modello caricato lazy e condiviso una sola volta; import, input invalido e inference failure degradano a `Silero VAD unavailable -> legacy fallback`; wakeword, STT, TTS, barge-in e selezione microfono non sono stati riscritti. Test VAD: 15/15; test audio/STT/wakeword/barge-in/post-barge: 33/33; regressione completa dopo integrazione: 511/511 `OK`; compileall, Ruff e `pip check` superati. Smoke test microfono reale 16 kHz: 93 frame/93 score, modello disponibile; nessuna frase parlata interattiva A-G è stata eseguita, quindi Silero non viene dichiarato completamente validato end-to-end con voce umana. Checkpoint finale: `backups/silero-vad-final_20260902_174414.zip`, SHA-256 `CD37963A58F47DC30B0B059928DC7BF07CC43424C2E2A1369FD478BD4E88B4A2`.

Silero VAD integration baseline (2026-09-02): checkpoint recuperabile `backups/silero-vad-baseline_20260902_173055.zip`, SHA-256 `7356428A23429CD2AEA542C48F167E44E6740EAEACFA121778C383BFB289B524`. Il VAD esistente e WebRTC VAD dentro `voice.ascolta()` con RMS adattivo, pre-buffer e timeout; Silero verra integrato come provider opzionale ibrido, senza toccare wake word, STT, TTS, barge-in o selezione microfono.

OpenHands GUI lifecycle final (2026-09-02): `main.py` collega `QApplication.aboutToQuit` allo shutdown centrale; `CoreRuntime.stop()` e idempotente e isola gli errori per cleanup; `LocalServicesManager` conserva ownership per sessione e ferma OpenHands solo se avviato dalla sessione corrente. Verifiche: test mirati 13/13, suite completa 495/495 `OK`, compileall e `pip check` superati. E2E GUI reale: session-owned OpenHands fermato entro 15 s (`3000=False`); external-owned preesistente lasciato attivo (`3000=True`); SearXNG `8088=True` e Ollama `11434=True` in entrambi. Checkpoint finale: `backups/openhands-lifecycle-final_20260902_170157.zip`, SHA-256 `8F63DF12EE3C14524F3E49BB3A37E5F802AE2C8FC73DCCF344AE5154B1D8768C`.

OpenHands GUI lifecycle baseline (2026-09-02): prima della correzione del percorso `main.py -> Qt -> CoreRuntime -> LocalServicesManager` e stata creato il checkpoint recuperabile `backups/openhands-lifecycle-baseline_20260902_164331.zip` (SHA-256 da riportare nel checkpoint finale).

LiteLLM argument validation and retry precedence (2026-09-01): la causa del primo `{}` era lo schema opaco del solo campo `arguments_json`, che non esponeva al planner i required args della skill. `SkillRegistry` usa ora `inspect.signature` dell'handler registrato come schema canonico: gli argomenti mancanti o sconosciuti restituiscono `invalid_tool_arguments`, `missing_required_arguments` e `invocation_not_started` senza invocare Python. Il router deterministico riconosce le richieste LiteLLM complete, costruisce `model`/`prompt`/`max_tokens`, e la risposta finale usa esclusivamente `dati.text`. Nello stesso turno, una risposta verificata della stessa coppia tool/skill supera gli errori di pre-validazione anche con argomenti corretti diversi; gli errori di esecuzione reali e quelli di tool differenti restano visibili. Alias `input` è normalizzato solo per LiteLLM. Verifiche: 7 test mirati LiteLLM/registry, suite completa 483 test `OK`, Ruff, compileall source-scoped e `pip check` superati. E2E provider reale non eseguito per non usare API key; nessun segreto stampato o loggato. Checkpoint finale: `backups/litellm-argument-validation-retry-final-20260901.zip`.

LiteLLM argument normalization and retry precedence (2026-09-01): `litellm.complete` mantiene lo schema `model`/`prompt`/`max_tokens`; l'alias legacy `input` viene normalizzato a `prompt` solo per questa skill e gli argomenti sconosciuti sono rifiutati prima dell'handler. L'aggregatore operativo correla `tool/skill + argomenti canonici`: un retry riuscito sostituisce il failure correlato, senza nascondere errori di tool differenti; il campo `dati.text` di LiteLLM è usato nella risposta finale. Verifiche: 4 test LiteLLM/retry, suite completa 480 test `OK`, Ruff e `pip check` superati; `compileall` source-scoped superato. La scansione indiscriminata della directory `tests` aveva incontrato un file legacy Python 2 nell'ambiente `.expansion-env` (`olefile2.py`), escluso correttamente dalla verifica sorgente. Checkpoint finale: `backups/litellm-normalization-retry-final-20260901.zip` (SHA-256 `2EEA5078426580B591846EE55285419B5AEE6E6DDF59A1AD0237D3A861E91A32`).

Natural Keyring delete routing (2026-09-01): esteso il routing Expansion di `secrets.delete` con riconoscimento semantico delle azioni `elimina`/`cancella`/`rimuovi` e dei target Keyring/Credential Manager/credenziale/segreto/password/token/API key. Le richieste con `service` e `username` completi precedono router conversazionale e operational follow-up; quelle incomplete restano fail-closed senza parametri inventati. `secrets.delete` rimane sensitive: una sola pending nel `SkillRegistry`, conferma con argomenti originali e `keyring_delete` reale, annullamento senza side effect. Nessuna skill di lettura segreti aggiunta. Verifiche mirate: 42 test superati; suite completa: 476 test, `OK`; Ruff file modificati, `compileall` e `pip check` superati. Checkpoint finale: `backups/keyring-delete-routing-final-20260901.zip` (SHA-256 `0C600C227D1CD7712F15D670036EBE323696B6B46924F31FEAF9BC2E8F761DA2`).

Capability permission semantics (2026-09-01): corretto il gate tra `permission_manager`, `CoreRuntime` e `SkillRegistry`. La decisione capability conserva ora `allow`/`confirm`/`deny`: soltanto `deny` blocca la capability; `confirm` viene combinato con la risk policy e produce la pending centrale della singola action. `secrets.store` continua a richiedere una sola conferma sensitive e, dopo `Confermo`, esegue il `keyring_set` reale; annullamento, MCP sensitive e Ruff `fix=True` restano protetti, mentre `fix=False` resta read-only. Verifiche mirate: 42 test superati; suite completa: 472 test, `OK`; Ruff sui file modificati, `compileall` e `pip check` superati. Checkpoint sorgente finale: `backups/permissions-capability-semantics-final-20260901.zip` (SHA-256 `F4194C8F13B67391FC2EF77C549C8D6BB229CCDBC2DDEF865F3452521F0F7CE1`).

Keyring versus operational follow-up routing final (2026-09-01): precedence alle skill Expansion esplicite e autosufficienti; detector follow-up limitato ai riferimenti a un risultato precedente; `secrets.store` manifest-backed per keyring/credential manager con conferma sensitive staged, una sola pending e riuso degli argomenti originali; secret redatto da diagnostica, audit e OperationalContext. Verifiche: 65 test mirati e suite completa `467 test, OK`; Ruff sui file modificati, `compileall` e `pip check` superati. Il controllo Ruff globale include 66 segnalazioni preesistenti in helper/test legacy; nessuna segnalazione sui file modificati. Archivio sorgente recuperabile `backups/POST_KEYRING_OPERATIONAL_ROUTING_FINAL_20260901_030415.zip` (esclude ambienti, build, dist, cache, dati runtime, backup e `.env` reale).

Keyring versus operational follow-up routing baseline (2026-09-01): checkpoint sorgente recuperabile `backups/PRE_KEYRING_OPERATIONAL_ROUTING_BASELINE_20260901_023511.zip` (74.720.442 byte, SHA-256 `D9E6336D2372398ABA6F164C849EAC37FFCDFBAB3CCB7A2987C631ACAB870E24`; esclude ambienti, build, dist, cache, dati runtime, backup e `.env` reale). La correzione deve dare precedenza alle skill esplicite autosufficienti, mantenere `secrets.store` sensitive e redigere il valore del secret in ogni diagnostica.

MCP confirmation handshake final (2026-09-01): il loop agent si arresta immediatamente su `requires_confirmation=True`; il modello non puo' auto-confermare o creare una seconda pending; `action_id`/`confirmed` sono rifiutati come metadati MCP non autorizzati. La pending di `mcp.call` resta nel SkillRegistry centrale, mentre conferma e' eseguita dal percorso centrale con argomenti originali. E2E reale `jarvis_test.somma(a=17,b=25)`: primo turno una pending, secondo turno `successo=True`, `verified=True`, risultato `42`, pending consumata, risposta `Risultato MCP: 42`. Verifiche: 71 test mirati, suite completa `462 test, OK`, Ruff, `compileall` e `pip check` superati. Archivio sorgente recuperabile `backups/POST_MCP_CONFIRMATION_HANDSHAKE_FINAL_20260901_021833.zip` (132.512 byte, SHA-256 `3A87499A355F119139AAC7B82725C2E629D70520927B39A518875D23D5966406`; esclude ambienti, build, dist e dati runtime).

MCP confirmation handshake baseline (2026-09-01): prima della correzione dell'auto-conferma nello stesso turno e della risposta finale MCP era stato creato il checkpoint sorgente recuperabile `backups/PRE_MCP_CONFIRMATION_HANDSHAKE_20260901_021056.zip` (131.157 byte, SHA-256 `ED9C1C4B1ABB7B6AFBF3298274EDC7F1CC37FAF5121D07E29438059B157F95FC`; esclude ambienti, build, dist e dati runtime).

Operational confirmation and Ruff policy final (2026-09-01): pending action condivise tra action guard e SkillRegistry con action_id, tool/skill, argomenti completi, timestamp, rischio effettivo e stato; conferma/annullamento espliciti, scadenza e selezione tra piu azioni; `ruff.check(fix=False)` read-only senza conferma e `fix=True` con conferma dinamica. Suite completa `459 test, OK`; 68 test mirati, Ruff, `compileall` e `pip check` superati. E2E reale read-only: F401 rilevato sul file di test senza modificarlo; E2E fix isolato: stesso path/argomenti, conferma verificata e pending svuotato. Archivio sorgente recuperabile `backups/POST_OPERATIONAL_CONFIRMATION_RUFF_POLICY_FINAL_20260901_015143.zip` (131.151 byte, SHA-256 `AF51EF427D304586608C46BE49585123B6A6F879A541AAEEB520992A19C42554`; esclude ambienti, build, dist e dati runtime).

Operational confirmation and Ruff policy baseline (2026-09-01): prima della correzione del loop di conferma Expansion e della distinzione `ruff.check(fix=False/True)` e stato creato il checkpoint sorgente recuperabile `backups/PRE_OPERATIONAL_CONFIRMATION_RUFF_POLICY_20260901_011347.zip` (109.646 byte, SHA-256 `D909D6367F255708D72F036F61CD15E189B7904A6F5C2DE03921FC5394AC2948`; esclude ambienti, build, dist e dati runtime).

Qdrant observability and fast-path response final checkpoint (2026-09-01): fast path deterministico integrato nella diagnostica condivisa `TOOL/ARGOMENTI/RISULTATO`; `qdrant.add` ora risponde con conferma specifica solo dopo read-back verificato; `qdrant.search` espone i payload reali e il contesto operativo conserva il testo trovato anche dentro `dati.points`. Verifica finale: 12 test Qdrant mirati, suite completa `453 test, OK`, Ruff, `compileall` e `pip check` puliti. E2E reale locale eseguito con sidecar e Qdrant embedded. Archivio sorgente recuperabile `backups/POST_QDRANT_OBSERVABILITY_20260901_010059.zip` (103.699 byte, SHA-256 `621B358D5038BA5EC67AF5BCA05544066AF3C3A1BE377F2FEA144E5048670A12`; esclude ambienti, build, dist e dati runtime).

Qdrant observability and fast-path response baseline (2026-09-01): prima della correzione della diagnostica `TOOL/ARGOMENTI/RISULTATO` e della risposta finale di `qdrant.search` e stato creato il checkpoint sorgente recuperabile `backups/PRE_QDRANT_OBSERVABILITY_20260901_005218.zip` (99.940 byte, SHA-256 `F8876E875FA5A602CFDF8294CB5A8CF1791D74E2D4EB0DAA65BA5AB83BED7340`; esclude ambienti, build, dist e dati runtime).

Qdrant routing and general false-confirmation protection final checkpoint (2026-09-01): routing Expansion basato sui manifest, fast path deterministico Qdrant, verifica read-back locale e gate anti-conferma falsa implementati. Verifica finale: 9 test Qdrant mirati, suite completa `450 test, OK`, Ruff pulito sui file modificati, `compileall` riuscito e `pip check` senza dipendenze rotte. Archivio sorgente recuperabile `backups/POST_QDRANT_FALSE_CONFIRMATION_20260901_004106.zip` (99.936 byte, SHA-256 `17EACC228EA542EE44B04BB3D55D03B4B58B83427DF36299EB44DD593380E65E`; esclude ambienti, build, dist e dati runtime).

Qdrant routing and general false-confirmation protection baseline (2026-09-01): prima della correzione è stato creato il checkpoint sorgente recuperabile `backups/PRE_QDRANT_FALSE_CONFIRMATION_SOURCES_20260901_001200.zip` (2.932.130 byte, SHA-256 `3DE942DB06046A95080B9A4E923B2C8E25A0B172C13F975543A7DD0F4C6B9814`; include sorgenti, configurazione, documentazione e test; esclude ambienti, build, dist, dati runtime e dipendenze vendorizzate).

Operational follow-up correction final checkpoint (2026-08-31): implementazione verificata con 12 test mirati e suite completa `441 test, OK`; `pip check` senza dipendenze rotte. Archivio sorgente recuperabile `backups/OPERATIONAL_FOLLOWUP_FINAL_20260831_184902.zip` (12 file, 107.893 byte, SHA-256 `AD72474F610345E2D32BCFD768455B4275D103E5AF633D0EDE6FE88F689E4B9A`; esclude ambienti, build, dist, dati runtime e `.env` reale).

Operational follow-up correction baseline (2026-08-31): prima dell'estensione del contesto operativo e della protezione anti-falsa-conferma e stato creato il checkpoint sorgente recuperabile `backups/PRE_OPERATIONAL_FOLLOWUP_SOURCES_20260831_183721.zip` (17 file, 116.050 byte, SHA-256 `CC771A5A8655C56B559F2B1FF5878D9375018CFABE4B13687AEDAE9F063905B8`). L'archivio esclude ambienti, build, dist, dati runtime e `.env` reale.

Development auto-CEO baseline (2026-08-31): prima della modifica temporanea che scollega l'identita dal runtime e stato creato il checkpoint sorgente recuperabile `backups/pre_auto_ceo_development_sources_20260831.zip` (8 file, 60.276 byte). Il repository non contiene una directory Git; il checkpoint include i file coinvolti, i test pertinenti, la guida operativa e questo stato.

Development auto-CEO verification (2026-08-31): `startup_identity_check()` ora pulisce e ricrea sempre la sessione volatile OWNER/CEO con metodo `development_auto_ceo`; il preload la attiva prima dei servizi opzionali; i comandi di profilo/biometria sono intercettati senza usare IdentityService; il gate di autenticazione vocale del wake word e bypassato solo per questa sessione. Test mirati: 27/27 superati; compileall e `pip check` superati. Suite completa: 429 test eseguiti, 427 superati e 2 fallimenti preesistenti non correlati in `test_agent_router` e `test_provider_router`, riprodotti anche singolarmente.

Development auto-CEO final checkpoint (2026-08-31): archivio sorgente recuperabile `backups/auto_ceo_development_final_20260831.zip` (8 file, archivio verificato).

Local router verification baseline (2026-08-30): prima della correzione di Chrome, chiusura programmi e conferme post-azione è stato creato il checkpoint recuperabile `backups/PRE_LOCAL_ROUTER_VERIFICATION_20260830_211700.zip`.

Local router verification implementation (2026-08-30): Chrome ora usa il bridge DOM quando disponibile ma ricade sull'avvio nativo senza segnalarlo come fallimento; Blocco Note e gli altri programmi vengono chiusi prima con WM_CLOSE e poi, solo se necessario, con taskkill, verificando infine che processo e finestra siano spariti. Rimossi gli annunci vocali anticipati: JARVIS parla dopo l'esecuzione. Se un tool fallisce, il testo finale non può più trasformare l'errore in "Fatto". Il report capacità espone ora funzioni disponibili, condizioni e limiti.

Local router verification verification (2026-08-30): 48 test mirati e 426 test completi superati; compileall, ruff sui file modificati e `pip check` superati. L'EXE Desktop aggiornato è rimasto vivo per 12 secondi e i processi generati sono stati chiusi senza residui.

Local router verification final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_LOCAL_ROUTER_FINAL_20260830_213817.zip`, 118.422.703 byte, 631 elementi, senza `.env` reale (contiene solo `.env.example`). EXE consegnato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`, 274.598.594 byte, SHA-256 `050D4732804A3311281ED161307B9F06DADEC269017AB8B0339E92761386F69B`.

Continuous voice recovery baseline (2026-08-30): prima della correzione STT e stato creato il checkpoint recuperabile `backups/PRE_VOICE_RECOVERY_20260830_204450.zip`.

Continuous voice recovery implementation (2026-08-30): la trascrizione OpenAI usa ora un tentativo bounded da 20 secondi senza retry; se il provider fallisce o restituisce testo vuoto, il testo Vosk gia acquisito viene mantenuto come fallback. Il file WAV temporaneo viene eliminato anche quando la chiamata remota genera un'eccezione. La calibrazione VAD considera solo frame classificati come silenzio, evitando di scambiare la prima frase dell'utente o la coda dell'audio TTS per rumore.

Continuous voice recovery verification (2026-08-30): 12 test vocali mirati e 418 test completi superati; compileall, `pip check` e ruff sui file modificati superati. L'EXE Desktop aggiornato e rimasto vivo per 12 secondi con auto-discovery del `.env`, quindi e stato chiuso senza processi residui.

Continuous voice recovery final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_VOICE_RECOVERY_FINAL_20260830_205356.zip`; EXE consegnato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`, 274.595.522 byte, SHA-256 `6C4343B0B4EDB393D33497447EF7FE98AC9EB1ED4A063567851779F78800F85A`.

Mini-orb quality refinement baseline (2026-08-30): prima del ridimensionamento e dell'ottimizzazione DPI e stato creato il checkpoint recuperabile `backups/PRE_MINI_ORB_QUALITY_20260830_201904.zip`.

Mini-orb quality refinement implementation (2026-08-30): il mini-orb del percorso `MainWindow -> MinimizedOrb` ora misura 72x72 px, resta in basso a destra e usa lo stesso master RGBA 4K dell'orb principale. `OrbWidget` ridimensiona il master alla densita fisica reale del monitor con `SmoothTransformation` e una cache bounded per evitare bitmap a bassa qualita o ricampionamenti ripetuti.

Mini-orb quality refinement verification (2026-08-30): test HUD mirati 14/14 e regressione completa 416/416 superati; compileall, `pip check` e ruff sui file modificati superati. L'EXE Desktop aggiornato e rimasto vivo per 12 secondi usando la scoperta automatica del `.env` in `Desktop\Jarvis2.0\.env`; i processi generati sono stati chiusi senza residui.

Mini-orb quality refinement final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_MINI_ORB_QUALITY_FINAL_20260830_202631.zip`; EXE consegnato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`, 274.594.418 byte, SHA-256 `15BF021C63F90E5E91E2629CCF16D55E4005429FF06997B7FC7F3853197BE80E`.

API keys + minimized orb + startup greeting baseline (2026-08-30): prima dell'intervento e stato creato il checkpoint recuperabile `backups/PRE_API_KEYS_MINI_ORB_GREETING_20260830_193323.zip`.

API keys + minimized orb + startup greeting implementation (2026-08-30): il gateway carica le variabili dal `.env` del progetto, da quello accanto all'EXE o dal percorso `Desktop\Jarvis2.0\.env` quando l'EXE e sul Desktop; OpenAI e Kimi vengono inizializzati al primo uso, mentre Claude e ElevenLabs leggono lo stesso ambiente senza includere segreti nel pacchetto. Aggiunto `.env.example` con i nomi supportati. `MainWindow` mostra ora un mini-orb 108x108 in basso a destra quando e minimizzata e lo usa per il ripristino. Il saluto iniziale e protetto da un gate per processo, quindi "Sistema pronto. Come posso aiutarti oggi?" viene annunciato una sola volta.

API keys + minimized orb + startup greeting verification (2026-08-30): 45 test mirati e 416 test completi superati; compileall, `pip check` e ruff sui file modificati superati. Le configurazioni presenti risultano riconosciute per OpenAI, Claude, Kimi ed ElevenLabs senza stampare i valori. L'EXE sul Desktop, avviato con il `.env` del progetto scoperto automaticamente, e rimasto attivo per 12 secondi ed e stato chiuso senza errori. La scansione ruff dell'intero repository resta non pulita per 85 violazioni preesistenti fuori da questo intervento.

API keys + minimized orb + startup greeting final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_API_KEYS_MINI_ORB_ENV_FINAL_20260830_201252.zip`; EXE consegnato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`, 274.594.857 byte, SHA-256 `57FD31F270A7B23D5DDD8E1B5FD5058C6697A29FF3815985B62824484A19FBE9`.

Static 4K orb + deferred credentials baseline (2026-08-30): prima della correzione dell'avvio senza chiave API e stato creato il checkpoint recuperabile `backups/PRE_CREDENTIALS_DEFERRED_20260830_191001.zip`.

Static 4K orb + deferred credentials implementation (2026-08-30): `llm_gateway.py` carica il file `.env` solo dall'ambiente del progetto/eseguibile senza inserirlo nel pacchetto e rimanda la creazione del client OpenAI al primo uso reale. Se `OPENAI_API_KEY` manca, il bootstrap HUD/voce non va piu in crash; le funzioni remote restituiscono invece un errore esplicito e controllato quando vengono richieste.

Static 4K orb + deferred credentials verification (2026-08-30): 19 test mirati e 412 test completi superati; `ruff`, compileall e `pip check` superati. L'EXE standalone e stato avviato senza credenziali OpenAI e il processo e rimasto attivo per 12 secondi.

Static 4K orb + deferred credentials final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_STATIC_4K_CREDENTIALS_FINAL_20260830_191657.zip`; EXE consegnato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`, 274.591.438 byte, SHA-256 `B82DCE9A502B45566DE308F5539BF726F2DB2A7003D49862FF50399AF554F8A8`.

Static 4K orb baseline (2026-08-30): prima di rimuovere l'animazione volumetrica e stato creato il checkpoint recuperabile `backups/PRE_REMOVE_ORB_ANIMATION_20260830_185453.zip`.

Static 4K orb implementation (2026-08-30): rimossi completamente timer, prospettiva dinamica, hotspot, orbite e deformazioni del renderer. `OrbWidget` mostra ora il solo master 4K trasparente `assets/orb/orb_idle_realistic_4k_transparent.png`, identico e stabile in Home e pre-avvio; la logica di stato resta compatibile ma non cambia la grafica.

Static 4K orb verification (2026-08-30): 18 test HUD/visuali e 411 test completi superati; render Home/Startup puliti, `ruff`, compileall e `pip check` superati. EXE standalone aggiornato e avviato con successo per 10 secondi.

Static 4K orb final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_STATIC_4K_FINAL_20260830_190350.zip`; EXE consegnato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`, 274.589.705 byte, SHA-256 `BD5B963F0EE70D525CDE80FE354C08F916E5066B482E9EF2C04499708030ACB9`.

4K animated orb baseline (2026-08-30): prima del nuovo passaggio volumetrico e stato creato il checkpoint recuperabile `backups/PRE_4K_ORB_ANIMATION_20260830_181758.zip`.

4K animated orb implementation (2026-08-30): il master canonico e ora `assets/orb/orb_idle_realistic_4k_transparent.png`, RGBA 4096x4096, derivato dal render 3D approvato senza cambiare identita, testo o palette. `OrbWidget` aggiunge animazione permanente a 60 fps con parallax/prospettiva piu evidente, luce e ombra dinamiche, hotspot speculare, orbite ellittiche e riflesso frontale; Home e pre-avvio usano lo stesso renderer e lo stesso master.

4K animated orb verification (2026-08-30): 18 test HUD/visuali e 411 test completi superati; il test dedicato verifica che due frame abbiano pixel differenti. Render Home/Startup verificati, `ruff`, compileall e `pip check` puliti. La nuova build PyInstaller `--onefile` e stata avviata con successo per 12 secondi.

4K animated orb final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_4K_ANIMATED_FINAL_20260830_182352.zip`; EXE consegnato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`, 274.540.601 byte, SHA-256 `448C6CD4576FC4D95DD6A68791E7A0CC09EBCB382507B8DEAE10174FE3398462`.

Standalone EXE build baseline (2026-08-30): prima della pacchettizzazione e stato creato il checkpoint recuperabile `backups/PRE_EXE_BUILD_20260830_175738.zip`, escludendo ambienti e output di build.

Standalone EXE build implementation (2026-08-30): compilata prima una build PyInstaller di controllo da `Jarvis2.0\jarvis_entry.py`, poi una build `--onefile --windowed` con tutte le dipendenze applicative e gli asset correnti. Il file standalone e stato copiato in `C:\Users\gabri\Desktop\JARVIS2.0.exe`; non dipende dalla vecchia cartella `Desktop\Jarvis`.

Standalone EXE build verification (2026-08-30): EXE creato di 264.707.905 byte, SHA-256 `7B3C08982B7F40452138A99A543380781C07742F16B8140931898047D43B6713`; hash della copia sul Desktop coincidente. La build di controllo contiene `assets/orb/orb_idle_realistic_v3_transparent.png` con hash identico al sorgente; smoke test del processo standalone superato per 12 secondi senza uscita anticipata.

Standalone EXE final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_EXE_STANDALONE_FINAL_20260830_180527.zip`.

Animated 3D orb baseline (2026-08-30): prima di aggiungere il movimento continuo e stato creato il checkpoint recuperabile `backups/PRE_ORB_ANIMATION_20260830_174514.zip`.

Animated 3D orb implementation (2026-08-30): il master 3D canonico ora ha un loop ambientale permanente con trasformazione prospettica, pulsazione e sweep speculare; lo stato `listening` usa lo stesso orb con ciclo piu rapido e intenso. Home e pre-avvio condividono renderer, asset e profilo ambientale.

Animated 3D orb verification (2026-08-30): 17 test HUD/visuali superati, render Home/Startup puliti, lint, compileall e `pip check` superati. La suite completa resta parziale per un errore preesistente e non correlato nel test clipboard (`test_clipboard_failure_redacts_provider_details`, `KeyError: 'error'`).

Animated 3D orb final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_ORB_ANIMATED_FINAL_20260830_175138.zip`.

3D depth render baseline (2026-08-30): prima del nuovo master con guscio, anello cromato, nucleo arretrato e ombra di contatto e stato usato il checkpoint `backups/JARVIS2_SOURCE_LAUNCHER_FINAL_20260830_173251.zip`; il master precedente resta come fallback.

3D depth render implementation (2026-08-30): generato un nuovo master idle con resa 3D piu evidente e trasparenza pulita, poi impostato come unico asset canonico di `OrbWidget`. Home, pre-avvio e tutti gli stati dell'assistente usano lo stesso render statico, senza cambio di grafica.

3D depth render final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_ORB_3D_DEPTH_FINAL_20260830_173918.zip`; 17 test HUD/visuali superati, render Home/Startup verificati, lint e compileall puliti.

Launcher source tag baseline (2026-08-30): prima di rendere riconoscibile l'avvio di `Jarvis2.0` e stato creato il checkpoint recuperabile `backups/PRE_LAUNCHER_SOURCE_TAG_20260830_173233.zip`; la cartella separata `Desktop/Jarvis` non e stata modificata.

Launcher source tag implementation (2026-08-30): `Jarvis2.0/Avvia Jarvis.cmd` mostra ora il titolo `JARVIS 2.0 - SOURCE` e stampa il percorso esatto prima di eseguire `Jarvis2.0\.runtime-env\Scripts\python.exe Jarvis2.0\main.py`. Il launcher resta relativo alla propria cartella, quindi non puo confondere il codice con quello della vecchia cartella `Jarvis`.

Launcher source tag final checkpoint (2026-08-30): archivio recuperabile `backups/JARVIS2_SOURCE_LAUNCHER_FINAL_20260830_173251.zip`; launcher verificato staticamente, test completi precedenti 410/410, `pip check` pulito.

Locked 3D orb baseline (2026-08-30): prima di bloccare il percorso visivo e stato creato il checkpoint recuperabile `backups/PRE_ORB_LOCKED_3D_20260830_172522.zip`; il master 3D corrente e rimasto invariato.

Locked 3D orb implementation (2026-08-30): eliminato il cambio automatico verso i vecchi frame `listening`, `thinking` e `speaking`. Ogni stato attivo, Home e pre-avvio usa ora lo stesso identico master 3D statico condiviso; la logica di stato resta disponibile al runtime ma non puo piu cambiare grafica.

Locked 3D orb final checkpoint (2026-08-30): archivio recuperabile `backups/HUD_ORB_LOCKED_3D_FINAL_20260830_172627.zip`; 17 test HUD/visuali e 410 test completi superati, render Home/Startup verificati, lint e compileall puliti.

Canonical 3D orb sharing baseline (2026-08-30): prima del vincolo unico Home/Startup e stato creato il checkpoint recuperabile `backups/PRE_ORB_SHARED_CANONICAL_20260830_171953.zip`; il master 3D v2 non e stato modificato.

Canonical 3D orb sharing implementation (2026-08-30): `OrbWidget` espone un solo asset idle canonico e condivide la stessa `QPixmap` tra Home e pre-avvio. Aggiunta regressione che verifica l'identita dell'asset; la sfera mantiene quindi esattamente materiali, ombre, riflessi e trasparenza in entrambe le schermate.

Canonical 3D orb sharing final checkpoint (2026-08-30): archivio recuperabile `backups/HUD_ORB_SHARED_CANONICAL_FINAL_20260830_172217.zip`; 17 test HUD/visuali e 410 test completi superati, render e matrice DPI verificati, lint, compileall e `pip check` puliti.

Orb realism and Home accent refinement v2 baseline (2026-08-30): prima della nuova sostituzione grafica e del dettaglio sotto-orb e stato creato il checkpoint recuperabile `backups/PRE_ORB_REALISM_V2_20260830_170946.zip`; il master precedente resta disponibile come fallback.

Orb realism and Home accent refinement v2 implementation (2026-08-30): aggiornato il master idle con materiale 3D monocromatico piu fisico, riflessi speculari controllati, ombre interne e sfumature volumetriche; aggiunta una linea orizzontale sfumata e centrata sotto l'orb nella Home. Startup continua a riusare lo stesso `OrbWidget`, quindi il nuovo orb e presente anche durante il pre-avvio insieme alla progressione 0-100%.

Orb realism and Home accent refinement v2 final checkpoint (2026-08-30): archivio recuperabile `backups/HUD_ORB_REALISM_V2_FINAL_20260830_1714.zip`; test focalizzati 16/16, regressione completa 409/409, render Home/Startup e matrice DPI verificati, lint, compileall e `pip check` puliti.

HUD minimal rebuild baseline (2026-08-30): prima della ricostruzione completa è stato creato il checkpoint recuperabile `backups/PRE_HUD_REBUILD_20260830_163603.zip`; includeva `hud_ui`, gli adapter HUD/startup, `main.py`, i test UI e questo registro, escludendo ambienti/build output.

HUD minimal rebuild implementation (2026-08-30): la vecchia composizione a pannelli/dashboard è stata sostituita con una sola Home monocromatica full-canvas: orb 3D canonica invariata al centro, pulsanti `LOG` e `CONSOLE` separati in alto a sinistra, controlli minimizza/chiudi in alto a destra e orologio/data in basso a destra. Log e Console sono superfici dedicate con cronologia/output bounded, refresh non bloccante e chrome coerente. Startup ora riusa lo stesso `OrbWidget` e mostra la progressione interpolata 0–100% nella stessa palette nero/bianco/grigio.

HUD minimal rebuild final checkpoint (2026-08-30): archivio recuperabile `backups/HUD_MINIMAL_REBUILD_FINAL_20260830_1652.zip`; suite completa finale 409 test superati, compileall e `pip check` puliti, render Home/Startup/Log/Console verificati.

Orb realism refinement baseline (2026-08-30): prima della sostituzione grafica è stato usato il checkpoint `backups/HUD_MINIMAL_REBUILD_FINAL_20260830_1652.zip`; l’orb originale `assets/orb/orb_idle.png` è rimasta intatta come fallback.

Orb realism refinement implementation (2026-08-30): aggiunto `assets/orb/orb_idle_realistic_transparent.png`, render 3D monocromatico con vetro rifrattivo, chrome stratificato, nucleo volumetrico e glow inferiore; l’`OrbWidget` lo preferisce con fallback automatico al master precedente. La trasparenza è stata verificata nel render HUD per evitare qualsiasi riquadro di fondo.

Orb realism refinement final checkpoint (2026-08-30): archivio recuperabile `backups/HUD_ORB_REALISM_FINAL_20260830_170520.zip`; 409 test completi superati, 22 test HUD focalizzati superati, lint `hud_ui`, compileall e `pip check` puliti.

DPI/layout scaling fix baseline (2026-08-30): checkpoint verificato prima della modifica in `backups/PRE_DPI_SCALE_FIX_20260830_033623.zip` (SHA-256 `75363EFF44FAFF1F3A595B4C1B34B86908F05B095C29388F8499D27C4ABB7104`). Rilievo Qt/Windows: schermo fisico 1920x1080, logico 1536x864, Windows 125%, DPR 1.25, DPI logico 96 e fisico 113.73; nessun override `QT_*` attivo.

DPI/layout scaling implementation (2026-08-30): introdotto `hud_ui.viewport` come unica trasformazione uniforme dal design 1672x941; Home, Startup, Log e Console ricalcolano coordinate, font, raggi e padding con la stessa scala `min(width/1672, height/941)` e centrano il viewport con letterboxing. Orb mantiene asset nativi RGBA (master/idle 465x465, frame animati 768x768), aspect 1:1 e `KeepAspectRatio` + `SmoothTransformation` nel solo passaggio di rendering finale. Nessun override Qt DPI/OpenGL e nessun doppio scaling applicativo.

DPI/layout diagnostics (2026-08-30): generati `diagnostics/layout_1280x720.png`, `layout_1366x768.png`, `layout_1600x900.png`, `layout_1920x1080.png`, `layout_2560x1440.png`, relativi Startup snapshot, `layout_actual_monitor.png` e `layout_diagnostics.json`. Tutte le cinque risoluzioni mantengono Orb e marker Startup quadrati; monitor reale verificato con finestra produttiva fullscreen: 1536x864 logici / 1920x1080 fisici, scala finale 0.918172 logica e composita 1.147715 fisica.

DPI/layout verification (2026-08-30): test mirati 21/21; full unittest regression 408/408; compilazione source/test 310 file; `pip check` pulito; benchmark UI 60 secondi completato senza crash. Smoke EXE reale: Startup e Home catturati dalla finestra JARVIS, processo responsivo oltre 30 secondi; catture in `diagnostics/exe_startup_dpi.png` e `diagnostics/exe_home_dpi.png`.

DPI/layout exact build (2026-08-30): PyInstaller completato in `dist_reference_exact_release_20260830_035909\\JARVIS\\JARVIS.exe`, 18.461.759 byte, SHA-256 `0ECD0FB57FDA4BDE62F710EEBDABBC1474F91B939DEC4B2836BB5EB31987A4FF`; asset Orb e Startup presenti nel bundle.

DPI/layout final recoverable checkpoint (2026-08-30): `backups/JARVIS_DPI_SCALE_FINAL_20260830_041130.zip`, 491 entry, 103.266.082 byte, SHA-256 `EEDDE7DE5248D2ED57FDD1D170BB26538E2153E4968C526C4AFA8A3898473F93`; esclusi ambienti, build/dist, `data/`, `backups/`, cache, `.env` e bytecode.

Pixel-match reference baseline (2026-08-30): checkpoint pre-rebuild verificato in `backups/PRE_PIXEL_MATCH_HUD_20260830_024714.zip` (SHA-256 `C84AAA3068A0D87E7AEC83D0C4A3881AB1600D1176E2F736734A243345A34E54`). Reference allegata misurata a 1672x941; estratti `assets/orb/master/orb_reference_master.png` e `assets/startup/startup_orb.png`.

Reference visual passes (2026-08-30): completati 5 cicli screenshot/compare/correct su Home e Startup. Il runtime usa un solo `hud_ui.OrbWidget`, Orb idle nativa 465x465, 24 frame derivati per stato e StartupView con progress interpolato.

Reference diagnostics (2026-08-30): generati `diagnostics/reference_target_home.png`, `diagnostics/actual_home_final.png`, `diagnostics/home_reference_comparison.png`, `diagnostics/reference_target_startup.png`, `diagnostics/actual_startup_final.png`, `diagnostics/startup_reference_comparison.png`, `diagnostics/exe_home_reference_final.png` e `diagnostics/exe_startup_reference_final.png`.

Reference exact build (2026-08-30): PyInstaller completato in `dist_reference_exact_release_20260830_031854\\JARVIS\\JARVIS.exe`, 18,454,303 byte, SHA-256 `ED1ABD3C39B4EDF602314A6BA7CDB345D650624CA51F762781002EDBF73E10FB`. Smoke EXE reale: processo responsivo e vivo oltre 30 secondi; asset Orb/master/startup presenti nel bundle; catture Home/Startup riuscite.

Reference verification (2026-08-30): focused HUD/visual tests 17/17; full unittest regression 404/404; source compilation 260 file; `pip check` pulito; idle UI benchmark 60 secondi completato senza crash.

Final recoverable checkpoint (2026-08-30): `backups/JARVIS_PIXEL_MATCH_FINAL_20260830_032759.zip`, 2.825 entry verificate, 107.777.631 byte, SHA-256 `344256DDC6224DBCCD5C1C43409F681ABE26352BE169B9BFEF6D3F754831D7E0`; esclusi ambienti, build/dist, `data/`, `backups/`, cache, `.env` e bytecode.

Updated: 2026-08-30

- Visual rebuild baseline (2026-08-30): checkpoint pre-modifica verificato in `backups/PRE_VISUAL_REBUILD_20260830_021144.zip` (SHA-256 `C38B3E28DC5597C047268E3E5295C72891CAC7818AACFA0F7C5BC0564866636D`). Baseline inventory: Orb raster cromata con rings, renderer startup duplicato in `hud_startup.py`, asset family 24 frame per stato già presente ma non condivisa in cache.

- HUD visual fidelity surgery (2026-08-29): raffinato esclusivamente il renderer canonico `hud_ui.OrbWidget` con rim metallico stratificato, vetro fumé radiale, profondità interna, archi tecnici, micro-tick, ombra e lower glow; aggiornati solo i dettagli tipografici/stilistici condivisi di Home. Struttura HUD, facade, startup, routing, segnali, lifecycle, LogView e ConsoleView invariati. Checkpoint: `backups/hud_visual_surgery_pre_20260829_220710.zip`. Snapshot finale: `artifacts/hud_visual_surgery_final.png`.
- EXE refresh after HUD surgery (2026-08-29): build ufficiale PyInstaller completata. `dist/JARVIS/JARVIS.exe`, 18,467,516 byte, SHA-256 `5A44EA5121D8D6405B6E00FA980BE8058114E4CAD0EB82526B75986A919464CB`; smoke test isolato: processo rimasto attivo 8 secondi, PASS.

- Kimi real provider gate (2026-08-29): `MOONSHOT_API_KEY` loaded from the project `.env`; the centralized Kimi gateway completed a real streaming smoke request with a non-empty response. Kimi acceptance: PASS.

- Final manual gate update (2026-08-29): user confirmed wake word, VAD and barge-in qualitative checks PASS; all available acceptance gates are now PASS. Kimi was subsequently verified through the configured `MOONSHOT_API_KEY`.

- Multi-monitor acceptance update (2026-08-29): user confirmed the second-monitor test passed manually. The previous hardware blocker is cleared; the report now records the gate as PASS.

- Live hardware recheck (2026-08-29): Windows reports `HP TrueVision HD Camera` in status OK; OpenCV opened it, acquired a frame and released it successfully. The multi-monitor gate was subsequently confirmed PASS by the user in a manual session.

- Manual acceptance update (2026-08-29): user confirmed all available manual gates PASS, including audio interaction, browser/mouse/typing flows and HUD visual QA. Multi-monitor and webcam remain NON VERIFICATO because the second monitor and webcam are not connected in the validation session.

- Final reliability checkpoint (2026-08-29): fixed CP1252-incompatible Unicode console output in the continuous voice-cycle startup, listening, recovery and standby paths. This prevents a Windows console encoding error from killing the recovery loop. Full regression: 400 tests passed; project-source compilation passed; `pip check` clean. Checkpoint: `backups/PRE_FINAL_REBUILD_20260829_180209.zip`.

- Final build checkpoint (2026-08-29): PyInstaller build completed successfully and the packaged executable remained alive for the 8-second startup smoke test. EXE: `dist/JARVIS.exe`, SHA-256 `35F7C0ED28C609AE7844470DFC0718F5D4D2E432BBF7FBF65A0D4974EBD6CDA4`. Final repository archive created with `tar` and integrity-checked by listing: `backups/JARVIS_FINAL_STABLE_20260829_181654.zip`, SHA-256 `F44F562BA2EB005E3FF3DB489CFD26B85A5DEC7BF18A7F690F7F72B511FBE9AC`.

- Real provider smoke gates (2026-08-29): a minimal real OpenAI streaming request completed successfully through `chiedi_jarvis()`; a minimal real ElevenLabs synthesis completed successfully (`29,720` bytes) and the temporary audio file was removed. Kimi remains NON VERIFICATO because it is not configured. These are smoke tests, not a claim of exhaustive provider certification.

- Real Windows smoke gates (2026-08-29): temporary file write/read passed; Notepad opened the target file with an observable window title, the window closed successfully, and the temporary file was removed. Clipboard round-trip passed. Broader audio, mouse, browser and multi-monitor acceptance remains manual/NON VERIFICATO.

- Audio configuration gate (2026-08-29): `test_audio_integration.py` now runs safely under Windows CP1252 and completed successfully. A real input device was detected (`Device 1`); OpenAI-first STT settings, ElevenLabs configuration, transcriber import and TTS timing/stall diagnostics all passed. Acoustic quality, wake-word accuracy and barge-in quality remain manual/NON VERIFICATO.

- Real browser smoke gate (2026-08-29): Chrome headless started with an isolated temporary profile, returned DOM HTML for `about:blank` with exit code 0, and the temporary profile was removed. Browser click/form/download flows remain NON VERIFICATO.

- 500-cycle stress gate (2026-08-29): extended the permanent conversational-cycle regression from 30 to 500 synthetic cycles with one injected processing failure. Recovery was invoked exactly once, all 500 cycles returned to the bounded standby path, and the focused plus full 400-test regression passed.

Updated: 2026-08-20

- Local router empty-response recovery (2026-08-21): deterministic local commands are now always attempted even when `performance_mode` is disabled, and an empty successful provider stream now produces an explicit user-visible retry message instead of silently returning no response. Added regression coverage for the setting-independent local path. Full regression: 375 passed; `pip check` clean; `ai.py` and `brain.py` compile successfully. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,585,033 bytes, SHA-256 `B8AEC68717B93AFF07E5A93182EA448BD4408AD4A4C1D8CC292F81454ECA2085`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/local-router-empty-response-fix-20260821-003000.zip`.

- Football H2H weighting correction (2026-08-21): head-to-head data now contributes a bounded 0.10 factor to expected goals instead of affecting confidence only; recent form and team metrics remain dominant. Football tests: 5 passed; full regression: 374 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,585,003 bytes, SHA-256 `68F81137F75489FC1C34B51FB15BEC6FBBECAE93BEAF2A1A4F9ED6561C64A2CA`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/football-h2h-weight-correction-20260821-002000.zip`.

- Football multi-factor analysis (2026-08-21): replaced quote-only output with explainable weighted form, home/away profiles, expected goals, Poisson 1X2/over 2.5/BTS probabilities, fair odds, rest-day context, confidence and explicit data warnings. Added bounded Football-Data CSV collection (up to 5 seasons, 100 matches/team, 8 MB response, 15 s timeout) behind `collect_historical`; no automatic betting execution. Football tests: 6 passed; full regression: 374 passed; `pip check` clean. Added PyInstaller hidden import so the dynamic football module is included in the EXE. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,584,780 bytes, SHA-256 `98DA88B6B28DAB99A8B073D01EED2A9B8757AD9D5414E71C799A2EABCB14B521`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/football-analysis-packaging-fix-20260821-001000.zip`.

- Automation event-name normalization (2026-08-20): the automation bridge now safely normalizes null or non-string event names before matching, preventing malformed direct calls from interrupting event handling. Existing valid event routing is unchanged. Added regression coverage. Focused tests: 4 passed; full regression: 372 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,572,607 bytes, SHA-256 `3BA0F0F1FE43164DEDFF4AF90D3F2EA10186E55DE0340E42F42C9751D9DA4C01`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/automation-event-name-normalization-20260820-242000.zip`.

- Stale automation run recovery (2026-08-20): executions left `running` beyond 15 minutes are now safely re-reserved and retried once, while recent running executions remain idempotently protected. Added regression coverage for reuse of the existing unique reservation. Focused tests: 13 passed; full regression: 371 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,572,580 bytes, SHA-256 `C2E0243B7113F1A8771D05A4168F0C991C3FF6E991530A788F6E32256743AE3F`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/stale-automation-run-recovery-20260820-241000.zip`.

- Corrupt automation action isolation (2026-08-20): persisted automations with invalid `actions_json` are now excluded from listings and execution without hiding healthy automations or treating the corrupt row as an empty action list. Added regression coverage. Focused tests: 12 passed; full regression: 370 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,572,149 bytes, SHA-256 `BE1DB32963113F04E76C8404629AA46B0746957569972AC9B7C12F8C0CE80C47`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/automation-corrupt-actions-isolation-20260820-240000.zip`.

- Automation history limit validation (2026-08-20): history queries now normalize invalid limits to the safe default and clamp valid values to 1–500, avoiding failures from malformed UI or tool input. Added regression coverage. Focused tests: 11 passed; full regression: 369 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,572,066 bytes, SHA-256 `9A677D83DE3BE8521C1B1DD77352AF78B45D27CD597DFFAA84C2263EAA6858D8`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/automation-history-limit-validation-20260820-235000.zip`.

- Automation history corruption recovery (2026-08-20): malformed persisted `outputs_json` is now treated as empty output when reading history or returning a deduplicated prior run, preserving status, attempts and idempotency. Added regression coverage. Focused tests: 10 passed; full regression: 368 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,571,969 bytes, SHA-256 `E0B94121C06E6F4A9BCD9B24A55AF32D571DFB03ADB57996FA1BEB8AEA049252`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/automation-history-corruption-recovery-20260820-234000.zip`.

- Legacy schedule isolation (2026-08-20): `due()` now skips malformed persisted schedules instead of aborting the entire scheduler cycle; valid schedules continue to be returned. Added regression coverage with a corrupt legacy row. Focused tests: 9 passed; full regression: 367 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,571,777 bytes, SHA-256 `1A17E601CAA90B63F0CA6DFAA9C6F61D81C3004C9353FF9E56089F11536619DC`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/legacy-schedule-isolation-20260820-233000.zip`.

- Automation time-trigger validation (2026-08-20): `once`, `daily` and `cron` trigger values are now validated before persistence, preventing malformed schedules from failing later inside the scheduler. Valid flexible hour formats such as `8:30` remain accepted. Added regression coverage. Focused tests: 8 passed; full regression: 366 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,571,718 bytes, SHA-256 `A40DDF63E1182A2F57262BB3403B273D8DBF46B235B73F9463E810A06F5E67F4`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/automation-time-trigger-validation-20260820-232000.zip`.

- Automation interval validation (2026-08-20): the persistent automation engine now rejects zero, negative and non-numeric interval triggers at creation instead of silently coercing them to a one-second schedule. Added regression coverage; existing positive intervals are unchanged. Focused tests: 7 passed; full regression: 365 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,571,401 bytes, SHA-256 `8F01690AD1C0B161B55FD324DC5F8D8D3D23D8074E20726C3EEA342FF182333F`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/automation-interval-validation-20260820-231000.zip`.

- Corrupt configuration layer recovery (2026-08-20): an unreadable or partially written optional JSON layer is now skipped so valid layers and defaults can still initialize the runtime. Added regression coverage; invalid environment overrides and non-object configuration roots remain explicit errors. Focused tests: 3 passed; full regression: 364 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,571,199 bytes, SHA-256 `DF1A70C05F9EDC7242AE6140DB5CC1103B61172123621430B9C4A33620BA67C8`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/config-corrupt-layer-recovery-20260820-230000.zip`.

- Routine delay validation (2026-08-20): `add_after()` now rejects zero or negative delays before creating a routine, preventing unintentionally due actions. Added regression coverage; valid delayed routines are unchanged. Focused tests: 7 passed; full regression: 363 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,571,089 bytes, SHA-256 `3ED0FF33401260C16A4BC170F07CFA9FCC0417B6FDEFDEE83AC0CB78A3C05AC6`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/routine-delay-validation-20260820-225000.zip`.

- Event automation rule isolation (2026-08-20): malformed numeric values and inaccessible event-rule targets are now isolated to the affected rule; subsequent valid rules continue to be evaluated and receive a redacted notice. Added regression coverage, while preserving existing cooldown and dispatch behavior. Focused tests: 2 passed; full regression: 362 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,571,022 bytes, SHA-256 `A6800A36ECCDCB9A71AFB3FA6858B51A6036064A83C930F95D00A8DB5B53556B`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/event-rule-isolation-20260820-224000.zip`.

- Event automation command validation (2026-08-20): event rules now reject empty commands before persistence, preventing blank actions from being emitted by the worker. Added regression coverage. Focused tests: 4 passed; full regression: 361 passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,750 bytes, SHA-256 `2D71E4DBC687EBA815F327842DFBA7459FF54B617249B7C592FAA4C6F5AA16DF`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/event-rule-command-validation-20260820-223000.zip`.

- Anthropic Haiku model update (2026-08-20): centralized Claude default changed from the active `claude-sonnet-4-5` fallback to `claude-haiku-4-5-20251001`; `ANTHROPIC_MODEL` may override only the model name. The existing HTTPS Anthropic adapter, authentication header, API version, system/messages payload, max tokens, streaming, timeout and fallback behavior are unchanged. No reference to the retired Haiku model remains in project sources. Provider tests: 8 passed; full regression: 360 passed; `pip check` clean. Real Anthropic API call not performed; validation is static plus mocked SSE. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,681 bytes, SHA-256 `DA48197B44B9BF6FA1819A0B5E4673287CA023F22E7D3A4FC9D5EEEAE10A1A73`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/anthropic-haiku-20251001-20260820-221000.zip`.

- Atomic JSON state writes (2026-08-20): agent jobs, event rules and active context now write through PID-scoped temporary files and atomic replacement, preventing truncation after interruption while preserving existing formats and APIs. Full regression: 359 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,597 bytes, SHA-256 `3DCE1D4A9F6B2CB4011E7FA883D18B95F44F79689A8DDA874F94B1FC9883CB1C`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/atomic-json-state-writes-20260820-220000.zip`.

- Working-memory TTL snapshot correction (2026-08-20): fixed the defensive-copy sentinel edge case introduced in the previous snapshot fix; `snapshot()` now evaluates TTLs atomically, removes expired entries and preserves explicit `None` values. Added expiry regression coverage. Full regression: 359 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,132 bytes, SHA-256 `7103773751B244106986AD8BE5793294FB1DBC6BEE611C66D11A22EF715CFFC2`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/working-memory-ttl-snapshot-20260820-215000.zip`.

- Settings API return consistency (2026-08-20): `set_setting()` and `update_settings()` now return the normalized value/dictionary they actually persisted, eliminating divergence between API responses and live configuration. Added regression coverage. Full regression: 358 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,090 bytes, SHA-256 `5373CA900F3230FEB6B9509A12D43158D68E6EC25200A9404E0E68ADA4F56E7E`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/settings-api-return-consistency-20260820-214000.zip`.

- Final user-facing redaction sweep (2026-08-20): security notifications in the HUD and skill-registry failures now redact provider exception details before display or model-visible return. Full regression: 358 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,063 bytes, SHA-256 `B4E6000C29551DCB0C2B8498D680D7A70D64F03C054352FA378D3BE1E0EB6EB3`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/final-user-redaction-sweep-20260820-213000.zip`.

- HUD import lint clarification (2026-08-20): documented the intentional post-wrapper imports required by the monochrome `QColor` HUD adapter; first-party Ruff now reports zero findings. Full regression: 358 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,052 bytes, SHA-256 `7B9584484D64908596FC94D3023E158B8F5CC90CB33CA7922E81A49E22F6414C`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/hud-import-lint-20260820-212000.zip`.

- Settings default coverage (2026-08-20): added missing defaults for startup timeout and local-STT preferences, bringing their conversions under central type normalization. Added regression coverage. Full regression: 358 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,570,052 bytes, SHA-256 `4CE8CEEB424D060A833EF2CE70483021329F97B9FC1F5737EFD78AD24F51CB61`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/settings-default-coverage-20260820-211000.zip`.

- Settings write-path normalization (2026-08-20): `set_setting()` and `update_settings()` now pass through the same type normalization as file loading, preventing invalid values from entering the live cache after an API/UI write. Added regression coverage. Full regression: 357 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,569,980 bytes, SHA-256 `D0394DD5563A7A67512866796EE2052BDADF5240D037A49F04E572833929FBF0`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/settings-write-normalization-20260820-210000.zip`.

- Settings type normalization (2026-08-20): persisted scalar settings are now normalized centrally against their defaults; malformed numeric/boolean values cannot crash voice, HUD or identity conversions, while valid numeric strings remain supported. Added regression coverage. Full regression: 356 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,569,968 bytes, SHA-256 `DEF6E03113719C2E58E7D73EE5DAE832AC3A4B5DA0A120A8A69ED46EE87A2E16`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/settings-type-normalization-20260820-205000.zip`.

- Market worker setting fallback (2026-08-20): malformed `market_refresh_seconds` values now fall back to 60 seconds instead of terminating the market HUD worker after a failed refresh. Full regression: 355 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,569,271 bytes, SHA-256 `080F9211765DC1FDCC512CFAF212C574B2BB90F9AF89D26158B107A09A0F0AB3`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/market-worker-setting-fallback-20260820-204000.zip`.

- Network/clipboard boundary regression (2026-08-20): added redaction tests for DNS and clipboard provider failures; the DNS agent now handles generic `OSError` responses instead of letting them terminate the request. Full regression: 355 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,569,211 bytes, SHA-256 `0932659FE9FC1A455073E8322DF26E72A6FA51EB5EDDD7FA8543003A5C250CF5`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/network-clipboard-regression-20260820-203000.zip`.

- Remaining boundary redaction (2026-08-20): broker startup/request failures, network DNS failures, clipboard errors and biometric fallback errors now pass through the shared redactor before persistence or user-visible return values. Full regression: 353 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,569,005 bytes, SHA-256 `F0EC6E9D36FFA2F96EC3EB0DEC658D7E6E59C6FA46EBA1C03D11EF16A15F6F3F`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/remaining-boundary-redaction-20260820-202000.zip`.

- Working-memory snapshot consistency (2026-08-20): `WorkingMemory.snapshot()` now performs one read per key and preserves explicitly stored `None` values instead of dropping them; added regression coverage. Full regression: 353 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,568,996 bytes, SHA-256 `562FE9EAD2D62419DADC0373CE7E10298B57DB490E6A978CD44D2F08AC06AFF3`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/working-memory-snapshot-20260820-201000.zip`.

- HUD layout invariants (2026-08-20): all fixed-size HUD `zip()` mappings now use strict length checking, preventing silent truncation when labels, positions, or chart points drift out of sync. Full regression: 352 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,568,978 bytes, SHA-256 `3260DD32B77E79482ED487894B919B90DA7FA81C3794BE8B519831567976A5F3`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/hud-layout-invariants-20260820-200000.zip`.

- Static cleanup follow-up (2026-08-20): simplified inert HUD style strings, replaced an ambiguous local label variable, split compact developer/search imports, and suppressed unnecessary exception chaining for rejected workspace paths. Full regression: 352 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,568,958 bytes, SHA-256 `34D6BD7B5B9576C019C1C559121D66CA68333A45A2A637ED6B6D0569A691D6F6`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/static-cleanup-followup-20260820-195000.zip`.

- Startup HUD renderer fix (2026-08-20): removed the accidental second `MinimalStartupScreen.paintEvent` override that hid the complete orb/background/status renderer; the original full startup composition is now the active implementation. Removed one duplicate local math import. Full regression: 352 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,568,958 bytes, SHA-256 `34D6BD7B5B9576C019C1C559121D66CA68333A45A2A637ED6B6D0569A691D6F6`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/startup-hud-renderer-fix-20260820-194000.zip`.

- Static hygiene pass (2026-08-20): removed unused imports and dead local audio variables from legacy voice/wake-word paths plus unused imports in support modules; no runtime behavior was changed. Full regression: 352 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,568,925 bytes, SHA-256 `9A26DA669D05633EFC4E349D73BA63D4A692497E132CAD2CFE703348BAD23ABD`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/static-hygiene-pass-20260820-193000.zip`.

- Routine worker resilience (2026-08-20): the scheduled-routine HUD worker now survives transient storage errors, emits a redacted rate-limited diagnostic, and ignores empty commands instead of sending blank actions. Full regression: 352 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,569,028 bytes, SHA-256 `E7079BCB8571D70FF715FDE35C91385B7175C7A5B11CB2156F8BC7E6FC4F1131`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/routine-worker-resilience-20260820-192000.zip`.

- HUD worker diagnostics (2026-08-20): audio-level and system-monitor fallback paths now emit a redacted, rate-limited warning instead of silently swallowing failures; the UI fallback behavior is unchanged. Full regression: 352 tests passed; `pip check` clean. Rebuilt `dist/JARVIS/JARVIS.exe`, 18,568,812 bytes, SHA-256 `AFD177051E9CAE004BD81066EE6A2342C62BCAB8C4932BBF9B6C45075AE15C1D`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/hud-worker-diagnostics-20260820-191513.zip`.

- Final post-recovery verification (2026-08-20): automatic evaluation 10/10 PASS; trend `HEALTHY` across 8 reports with 0 regressions; acceptance audio input/output PASS, desktop UIA PASS, Calculator UIA PASS, File Explorer UIA PASS, single-monitor SKIP; `pip check` clean. Reports: `%LOCALAPPDATA%\\JARVIS\\evaluation_reports\\real-world-20260820-191033.json`, `data/acceptance/acceptance-20260820-191041.json`.

- Workspace integrity follow-up (2026-08-20): confirmed the only corrupted first-party source was the previously restored `football_analyst.py`; remaining small Python files are intentional package initializers/hooks/scripts. Full regression: 352 tests passed; `pip check` clean. Manual acceptance after the path guard: audio input/output PASS, desktop UIA PASS, Calculator UIA PASS, File Explorer UIA PASS, single-monitor SKIP. Runtime EXE remains the verified build SHA-256 `531CA21027AFAC0DDB40BD10B3FDF97C0C2486552AE272825F1231075CDC58BF`.

- Acceptance path guard regression (2026-08-20): added an automated test proving that the Windows desktop acceptance harness rejects output paths outside `data/acceptance`, preventing source-file overwrites. Full regression: 352 tests passed; `pip check` clean. Runtime EXE unchanged; latest verified package remains `dist/JARVIS/JARVIS.exe` SHA-256 `531CA21027AFAC0DDB40BD10B3FDF97C0C2486552AE272825F1231075CDC58BF`.

- Football module/acceptance recovery (2026-08-20): restored `football_analyst.py` after the interactive Notepad acceptance fallback had overwritten it with test text; rebuilt the advisory-only quote analysis module from the local bytecode baseline, restored 351-test health, and added an output-path guard forcing acceptance artifacts into `data/acceptance`. Manual acceptance: audio input/output PASS, desktop UIA PASS, Calculator UIA PASS, File Explorer UIA PASS, single-monitor SKIP. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,568,365 bytes, SHA-256 `531CA21027AFAC0DDB40BD10B3FDF97C0C2486552AE272825F1231075CDC58BF`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/football-acceptance-recovery-20260820-190536.zip`.

- Startup boundary error redaction (2026-08-20): biometric/guest-mode startup errors are now redacted before appearing on the startup screen. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,568,365 bytes, SHA-256 `531CA21027AFAC0DDB40BD10B3FDF97C0C2486552AE272825F1231075CDC58BF`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/startup-boundary-redaction-20260820-185646.zip`.

- Quality/STT error redaction (2026-08-20): project inspection issues and streaming-transcription diagnostics now redact exception details before entering project reports or voice error history. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,568,351 bytes, SHA-256 `851C83E100A0C61180A40E1C4039AAF10EB65F3C1E432E15B201D2F4856C86F2`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/quality-stt-redaction-20260820-185328.zip`.

- Workflow result error redaction (2026-08-20): file rollback, automation execution, emergency-stop callbacks and developer-lab transaction failures now redact exception details before entering structured results. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,568,243 bytes, SHA-256 `8B83CF21B5A92A7D98A4C165BB92A0DEF5DC412ADE4891DA5793A6FFB95529E2`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/workflow-error-redaction-20260820-184954.zip`.

- Mission/skill/perception error redaction (2026-08-20): structured failures from skill registry, mission execution and perception actions/state now redact exception details before entering model-visible results, task graphs or evidence. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,568,037 bytes, SHA-256 `30C9E2D9A19BC2C0D31EDF87DC5BC458B0D27B340C724660F4ACBB2069417858`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/mission-perception-redaction-20260820-184621.zip`.

- Operational history error redaction (2026-08-20): TTS session history, watchdog health failures and recovery attempt records now redact exception details before persistence or HUD/mission exposure. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,567,823 bytes, SHA-256 `E9B22573DFF1D94777EACD8E556A3EEEDD19A8ED7287C52DD80A7C88A398AC23`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/history-error-redaction-20260820-184259.zip`.

- Direct boundary error redaction (2026-08-20): Windows UIA agent, application launcher and autostart settings now redact exception details before returning direct results. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,567,691 bytes, SHA-256 `2942B84843D8E2B49C72A22565F52D20F5783A5167A4F8463B42D91C1AFADB62`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/boundary-error-redaction-20260820-183915.zip`.

- HUD user-facing error redaction and TTL test stability (2026-08-20): HUD system/audio/log/market messages and automation notices now redact exception details; the short WorkingMemory TTL regression test now waits with a safe margin to avoid timing flakiness. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,567,564 bytes, SHA-256 `7343EA2BEAD35763FB66065C991CF9F1CB782B5E06B197ED120C5562D0588A56`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/hud-user-error-redaction-20260820-183557.zip`.

- User-facing voice/profile error redaction (2026-08-20): profile, voice enrollment and recognition failures now redact exception details before local spoken/UI responses. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,567,353 bytes, SHA-256 `A76FEC03869CBAA67546DC3956C32E4FE790C451840F8883F08C2976701CD5A5`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/main-user-error-redaction-20260820-183141.zip`.

- HUD worker error redaction (2026-08-20): runtime snapshot and automation scheduler worker errors are now redacted before being emitted to HUD signals, closing a previously unprotected diagnostic boundary. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,567,252 bytes, SHA-256 `B739F51D7F27C6AE7A5E8DDFED936C274F60F2A30A59EDF26C4E8FF6182DC384`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/hud-worker-redaction-20260820-182822.zip`.

- Proactive monitor diagnostics (2026-08-20): disk/battery/RAM monitoring failures no longer disappear silently; they emit a structured warning with a 30-minute cooldown to preserve HUD/log signal quality. Added regression coverage. Full regression: 351 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,567,195 bytes, SHA-256 `36579BDCBAC61293A048FDD97EF4C0B7C5D48502FFCE8EE5DC39D984F3276F8B`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/proactive-diagnostics-20260820-182450.zip`.

- Post-fix behavioral verification (2026-08-20): real-world evaluation remains 10/10 PASS; trend analyzer is `HEALTHY` across 5 reports with 0 regressions; the latest 3,000 runtime log entries contain no unexpected ERROR/CRITICAL events (test-injected subscriber errors excluded). Latest report: `%LOCALAPPDATA%\\JARVIS\\evaluation_reports\\real-world-20260820-182254.json`.

- Notepad UIA acceptance hardening (2026-08-20): if ValuePattern updates the control but Windows Notepad does not commit the document, the acceptance harness now retries once through the focused editor and verifies the saved file as the authoritative postcondition. Manual acceptance: audio input/output PASS, desktop UIA PASS, Calculator UIA PASS, File Explorer UIA PASS, single-monitor SKIP. Full regression: 350 tests passed; `pip check` clean. Report: `data/acceptance/acceptance-20260820-182227.json`.

- Callback error redaction (2026-08-20): the pre-action callback diagnostic now redacts callback exceptions before console output. Full regression: 350 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,854 bytes, SHA-256 `852727415993B380374DB2DD574F78B9C8FBD3493C9A7ED797F0C5E63EF7A3CF`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/callback-error-redaction-20260820-181843.zip`.

- AI/startup error redaction (2026-08-20): AI/web streaming exceptions and bootstrap/wake-model failures are now redacted before console output or startup diagnostics persistence. Full regression: 350 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,839 bytes, SHA-256 `4D2FBADDF25C19597405B9EE6BD3947CB95F1FE299A45AA8314794022857B835`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/ai-startup-error-redaction-20260820-181548.zip`.

- Legacy tools error redaction (2026-08-20): program/site opening, program closing and Windows volume paths in `tools.py` now redact raw exceptions before console output. Full regression: 350 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,784 bytes, SHA-256 `7F5FB7D71320F67C370E5D4D15AC4521F6D98BE77EA5C23BE2E10A596CEB6C1D`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/tools-error-redaction-20260820-181244.zip`.

- Central desktop error redaction (2026-08-20): the shared `computer.errore()` response helper now redacts details for all desktop actions; planner/critic fallbacks and main diagnostic persistence were aligned as well. Added regression coverage. Full regression: 350 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,696 bytes, SHA-256 `5BF815BD014BD5C0114904A7E1F10B06969FEDEB859606AE3920950AAB63BB80`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/central-error-redaction-20260820-180934.zip`.

- Voice/async error redaction (2026-08-20): raw exceptions from audio output, barge-in, STT startup, microphone handling and asynchronous task history are now redacted before console/persistence. Full regression: 349 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,469 bytes, SHA-256 `691AF8FD2D2D6EBF1A261802697943E2E0B277AD1DEED39E318E06129C0C6837`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/voice-async-redaction-20260820-180536.zip`.

- Main-loop error redaction (2026-08-20): voice, AI-stream and PC-control exceptions printed by the application loop are now redacted before console output, completing the same protection at the user-facing runtime boundary. Full regression: 349 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,356 bytes, SHA-256 `A71E51E988C947EAE695F631544E58E128CBD729B533F34205053E16790AEC5D`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/main-error-redaction-20260820-180228.zip`.

- Skill error redaction (2026-08-20): direct failures from screen vision, TradingView analysis, network inspection and visual automation now redact exception content before returning it, aligning these modules with the central tool/UIA policy. Full regression: 349 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,282 bytes, SHA-256 `03FB1D45474A2B05575B94E34A65B54F054FCED20DFDA1222433996EBEBD7CC6`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/skill-error-redaction-20260820-175931.zip`.

- UIA error redaction (2026-08-20): structured desktop accessibility functions now redact provider/PowerShell exceptions both in direct results and audit records, closing the remaining UIA error path. Added regression coverage. Full regression: 349 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,085 bytes, SHA-256 `1CF9F11C62826006049AEEC3E91B2350C02470C9FD5C9BDC6EAE4CE33D01C0BE`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/uia-error-redaction-20260820-175616.zip`.

- Audit redaction refinement (2026-08-20): retry, router and critic-recovery exceptions are now redacted consistently before audit logging and console output, closing secondary error paths after tool-error hardening. Full regression: 348 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,566,018 bytes, SHA-256 `75FC77D8F5519AA11E3D8F59159E61BE5126CA13C54990394C063D8476C2FB67`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/minor-audit-redaction-20260820-175259.zip`.

- Minor security hardening (2026-08-20): tool exceptions are now redacted before being returned to the model or written to the audit stream, preventing accidental exposure of tokens/passwords embedded in provider or system errors. Added regression coverage. Full regression: 348 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,565,982 bytes, SHA-256 `1C7F9E1A6B156D75B85F447D7981DD3F9B2BA4B8C0FCAF14641DAD7BFCD5B39F`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/minor-redaction-20260820-174954.zip`.

- Diagnostic intent refinement (2026-08-20): natural requests such as “controlla le regressioni” and “analizza le valutazioni” now use the local trend analyzer directly, avoiding browser/UI side effects. Full regression: 347 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,565,935 bytes, SHA-256 `4066C19A5C5E4127A752F528E0426C248FB8D5681DB1C0220D80C1E453A6EBD8`; packaged smoke stayed alive for 8 seconds.

- Continuous improvement phase (2026-08-20): added bounded evaluation trend analysis with automatic regression detection, atomic trend reports, HUD diagnostics and user-facing dry-run/explicit approval for consolidated learned procedures. Procedures require two verified repetitions before approval and approval never executes actions. Full regression: 346 tests passed; `pip check` clean. Automatic evaluation: 10/10 PASS; trend status `HEALTHY` across 2 reports with 0 regressions. Latest report: `%LOCALAPPDATA%\\JARVIS\\evaluation_reports\\real-world-20260820-170552.json`; trend report: `%LOCALAPPDATA%\\JARVIS\\evaluation_reports\\trend-analysis.json`. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,565,342 bytes, SHA-256 `3E76AF4B90F09ED02387F2DC730627C6B8027EEE212AB8D611BBB9B60AD5CE2D`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/continuous-improvement-pre-20260820-170526.zip`.

- Real-World Evaluation Pack (2026-08-20): added `evaluation_pack.py` with ten automatic scenarios covering conversation, capability questions, direct desktop actions, structured UI, browser DOM, compound planning, ambiguity, local files, current information and recovery. Reports are atomic, bounded to decision/policy/provider/KPI evidence and never contain audio, screenshots or secrets. Automatic evaluation: 10/10 PASS; full regression: 343 tests passed; `pip check` clean. Current report: `%LOCALAPPDATA%\JARVIS\evaluation_reports\real-world-20260820-164148.json`. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,560,661 bytes, SHA-256 `8E6786206971C6E21AD2845B6F82F896637881FEE2729AD36E092E80343C8796`; packaged smoke stayed alive for 8 seconds. Checkpoint: `backups/real-world-evaluation-20260820-164349.zip`.

- TTS live fallback acceptance (2026-08-20): local fallback generated a valid 31,824-byte MP3 for an Italian sentence and completed pygame playback with `interrompibile=False`; no audio sample was persisted. Combined with the target-PC microphone/output acceptance and STT fragmentation repair, the current voice critical path has deterministic, real-device and packaged smoke evidence. Full regression remains 341 tests passed; `pip check` clean; current EXE remains 18,560,654 bytes with SHA-256 `81138F1FBCD854B70B51A11B93A81D0468A393FCE0F8F7E7E8451F256C210B7B`.

- STT fragmentation repair (2026-08-20): Vosk final chunks are now reassembled with a bounded Italian-word fusion pass, preventing mid-word finalization from producing spoken/transcribed artifacts such as `pronunc iare`. The path was tested deterministically and against the real `input.wav` sample (`apri impostazioni` recognized); no audio sample is persisted by the test. Full regression: 341 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,560,654 bytes, SHA-256 `81138F1FBCD854B70B51A11B93A81D0468A393FCE0F8F7E7E8451F256C210B7B`; eight-second packaged smoke stayed alive. Checkpoint: `backups/stt-final-20260820-162416.zip`.

- Target-PC end-to-end acceptance (2026-08-20): the corrected Notepad UIA scenario now commits the document with Ctrl+S and verifies the persisted file, eliminating the previous false-positive. Audio input/output discovery and a volatile two-second microphone sample passed; Calculator UIA, File Explorer UIA and authenticated broker inventory passed; one-monitor topology is explicitly SKIP. Reports: `data/acceptance/acceptance-20260820-161843.json` and `data/acceptance/acceptance-20260820-161923.json`. Full regression after the acceptance harness change: 340 tests passed; `pip check` clean. Checkpoint: `backups/acceptance-final-20260820-161956.zip`.

- Automation intelligence tranche (2026-08-20): added shared `automation_intelligence` policy for direct actions, structured UI, browser DOM, observe-and-act and clarification; injected the policy into the operational router; every tool result now receives standardized independent verification before returning to the model; procedural learning now accepts only completed missions whose successful steps have verified evidence and persists atomically. Full regression: 340 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,559,926 bytes, SHA-256 `BAFF28701088879D6DA9F3D2EADA903E8C99710FD73A2F6E7B3FF4FB711C8A13`; visible-window smoke stayed alive for 8 seconds. Checkpoint: `backups/automation-intelligence-20260820-161628.zip`.

- Full decision/orchestration integration (2026-08-20): the shared decision policy is now also injected into conversational AI instructions and provider classification. Configured provider routing remains capability-aware: OpenAI for Windows/browser tool execution and web-enabled operational paths, Claude first for coding/planning, Kimi first for long-context summarization, with availability-aware fallbacks. Full regression: 336 tests passed; `pip check` clean. Rebuilt EXE `dist/JARVIS/JARVIS.exe`, 18,556,701 bytes, SHA-256 `CE731061BAB7D17A573862935BC1AD2363DAC257BEA6F9C20F4660B5A0C5376E`; visible-window smoke remained alive after 8 seconds. Checkpoint: `backups/decision-layer-pre-20260820-154256.zip`.

- Decision layer integration (2026-08-20): checkpoint `backups/decision-layer-pre-20260820-154256.zip`. Added a shared deterministic intent policy used by the UI gate and the operational brain: conversation/information/capability/operation/composite/control classification, context awareness, observation requirement, strategy selection and destructive-operation flag. The policy is injected into the tool router so the model chooses tools according to a common contract: answer without tools when sufficient, observe before UI actions, prefer DOM/UIA over vision and mouse/keyboard, plan and verify compound work. Added six decision regression tests. Full regression: 336 tests passed; `pip check` clean.

- Finalizzazione usabilità (2026-08-20): creato checkpoint `backups/pre-finalizzazione-20260820-153703.zip`. Il router non classifica più le ricerche informative generiche come comandi desktop, evitando aperture/interazioni non richieste; le ricerche esplicitamente riferite a file restano operative. La coda TTS condivisa è ora bounded (32 richieste) e scarta le richieste obsolete a bassa priorità quando è piena, riducendo la latenza percepita. Regression suite: 330 test passati; `pip check` pulito. La qualità reale di microfono, cancellazione dell'eco e altoparlanti resta una verifica manuale sul PC dell'utente.

- Microphone standby repair (2026-08-19): the shared `input_stream` now accepts and validates explicit microphone IDs used by wakeword and STT, falling back to the Windows default when unavailable. Repeated standby failures are deduplicated for 30 seconds and exponentially backed off to prevent HUD log floods. Full gate: 320 tests; packaged EXE SHA-256 `6AA47915DE8ACB29E98CB611176192B5D561881FB17F60BE9E87FF2E4339EA5C`; 25-second packaged standby remained responsive with zero Windows crash/hang events.

- HUD responsiveness repair (2026-08-19): Command Center snapshots now run outside the Qt thread, hidden pages stop polling, duplicate refreshes coalesce, activity-log rendering is batched and bounded to 300 visible entries, and JSONL reads use a bounded file tail. Full gate: 318 tests, Ruff/Black/mypy and `pip check` clean. Packaged EXE SHA-256 `ACC4EB3BAF24A37228EEC6B80B6A7065186CF36C41A178863068EC67BDFEB449`; eight repeated LOG/HOME click cycles remained responsive and generated zero recent Windows crash/hang events.

## Verified baseline

- Voice/HUD checkpoint: `backups/voice_hud_pre_20260818_204433.zip`.
- Planner integration checkpoint: `backups/planner_integration_pre_20260818_203538.zip`.
- Recovery/emergency checkpoint: `backups/recovery_emergency_pre_20260818_201449.zip`.
- Privileged-broker checkpoint: `backups/privileged_broker_pre_20260818_195526.zip`.
- AI OS upgrade Phase 0 checkpoint: `backups/ai_os_upgrade_phase0_20260818_193815.zip`.
- Pre-Companion checkpoint: `backups/companion_pre_20260810_173554.zip`.
- Baseline command: `.\.runtime-env\Scripts\python.exe -m unittest discover -s tests -v`.
- Baseline result: 103 tests passed in 17.045 seconds; no failures.

## Components

| Component | Status | Evidence / next gate |
|---|---|---|
| Foundation runtime | COMPLETED | Existing config, JSON logging, EventBus, state, processes, health, watchdog and lifecycle tests pass. |
| Mission engine | COMPLETED | Existing graph, persistence, evidence, retry, cancellation and timeout tests pass. |
| Memory v2 | COMPLETED | Existing persistence, ranking, TTL, migration and graph tests pass. |
| Voice critical path | COMPLETED | Existing queue, priority, TTS metrics, barge-in and shutdown tests pass; hardware remains a manual gate. |
| Companion vertical slice | COMPLETED | Integrated and covered end-to-end: coding repeated-failure event, score, silence/focus policy, dedup, budget, shared interruptible TTS, normal conversation continuation, logs and metrics. Full automated regression passes; live microphone/speaker remains a manual hardware gate. |
| Companion context/activity monitoring | COMPLETED | Shared state, foreground/activity events, scoring, deduplication, cooldown and silence-first behavior are integrated and tested. |
| Coding Copilot | COMPLETED | Traceback, build, dependency, process and repeated-test-failure detectors are integrated with bounded suggestions. |
| Trading Copilot | COMPLETED | Authenticated TradingView DOM plus visual context is filtered and advisory-only; order execution remains prohibited. |
| HUD Companion surface | COMPLETED | Authoritative Companion, voice, task, broker, emergency and notification state is projected into the HUD. |
| Feedback/preferences | COMPLETED | Atomic layered configuration and distinct preference memory are integrated with sensitive-data rejection. |
| Long-duration/event-storm hardening | COMPLETED | Bounded queues, deterministic shutdown/restart and a 1,000-event storm regression prevent unbounded growth and repeated speech. |

## Current verification

- General reliability hardening (2026-08-19): bounded the GUI text-command queue with stale-item eviction, made legacy routine persistence atomic, rejected empty routine commands, and replaced clock-based routine IDs with collision-resistant IDs. Full regression: 310 passed, 1 environment-only skip; `pip check` clean. Checkpoint: `backups/full_audit_pre_20260819_183228.zip`.

- Audio listening hardening (2026-08-19): standby wake-word recognition now accepts only finalized Vosk hypotheses; active STT applies adaptive RMS noise gating in addition to VAD; microphone queues are bounded and drop stale frames; and barge-in starts only after TTS playback begins. These changes reduce false listening/transcription from ambient audio, TV and music and prevent stale-audio buildup. Focused voice tests: 14 passed; live microphone behavior remains a manual hardware gate.

- Reliability Engine tranche (2026-08-19): added `reliability_engine.ReliabilityEngine`, a bounded atomic mission-history service with versioned reports, success/first-attempt/recovery/tool-failure KPIs, retries, fallbacks, tool/provider/verification data, durations and recursive sensitive-field redaction. Checkpoint: `backups/reliability_engine_pre_20260819_164858.zip`. Focused tests: 2 passed; full regression: 310 passed, 1 environment-only skip; `pip check` clean. The service is ready for MissionEngine/EventBus adapter wiring; no claim is made that every legacy mission is automatically instrumented yet.

- Final target-PC acceptance (2026-08-19): `data/acceptance/acceptance-20260819-150926.json` proves audio input/output discovery, 1920x1080 display discovery, exact-HWND Notepad text/save, Calculator UIA, File Explorer UIA, UAC elevation, authenticated broker IPC, clean broker shutdown and successful system/driver/software inventory. Multi-monitor is skipped because this PC exposes one monitor; virtual-screen and multi-monitor geometry remain deterministically tested. Full regression: 304 passed, 1 environment-only DPAPI skip; scoped Black/Ruff, mypy (67 modern files) and `pip check` are clean. The packaged broker entrypoint also completed authenticated ping/stop under the real Windows identity. Final EXE: 18,420,526 bytes, SHA-256 `382C631AA7F98602326ECE1BA19C7C9505431A4E6982103CA4AF101E88D02A49`. Certified source checkpoint: `backups/final_certified_20260819.zip` (65,464,528 bytes; secrets, environments, runtime data, caches and build outputs excluded and archive inventory verified).
- ElevenLabs TTS integration (2026-08-19): `jarvis_voice.elevenlabs.ElevenLabsTTSProvider` is the primary provider when `ELEVENLABS_API_KEY` and `ELEVENLABS_VOICE_ID` are present. It uses `eleven_flash_v2_5`, internal voice defaults, PCM streaming at 44.1 kHz into the existing sounddevice path, bounded retries/timeouts, cancellation, formatter, cost optimizer, bounded cache, mood adjustments, metrics and non-secret HUD status. Missing or invalid credentials degrade to the existing local edge-TTS fallback. Full regression after integration: 308 passed, 1 environment-only DPAPI skip; the rebuilt EXE remained alive for eight seconds and its packaged broker passed authenticated ping/stop. Final EXE: 18,430,007 bytes, SHA-256 `FB907C322818499DA31982D3F20E4FC06800DB5298BD68B71D3AFC94B14CF87A`.

- Consent-driven hardware acceptance (2026-08-19): the launcher separately asks whether to capture a two-second microphone sample and whether to request the UAC broker check. Audio stays in memory, is zeroed after RMS/peak/noise/clipping calculation, and is never persisted; regression verifies metrics contain no samples. The local benchmark records p50/p95/p99/max evidence in `PERFORMANCE_REPORT.md`. Full regression: 298 passed with 1 environment-only DPAPI skip; quality and dependency gates remain clean. Checkpoint: `backups/tranche16_hardware_acceptance_20260819.zip`.

- Interactive-acceptance refinement (2026-08-19): exact-HWND parameters are exposed by the UIA skills, `keyboard.write` is now a first-class bounded Tool Registry capability, and package operations advertise Italian install/update/uninstall intents. The controlled acceptance runner now covers Notepad, Calculator, File Explorer, audio-device enumeration and display topology without ever selecting an unrelated new window. Audio input/output and display enumeration pass in the managed environment; desktop input remains stopped by PyAutoGUI's intentional corner fail-safe. Full regression: 297 passed with 1 environment-only DPAPI skip; Ruff, Black, mypy (66 files), and `pip check` clean. Rebuilt package: `dist/JARVIS/JARVIS.exe`, 18,415,205 bytes, SHA-256 `C6FBD74D07134D07090C799AC65FED1BB27281BE9B2D47CC84D35055C9851137`; eight-second smoke passed with six plugin manifests. Checkpoint: `backups/tranche15_tool_registry_acceptance_20260819.zip`.

- Interactive-acceptance tranche 14 (2026-08-18): UI Automation can now bind inspection and actions to an exact HWND; bounded keyboard text entry and a non-destructive interactive launcher (`Esegui collaudo JARVIS.cmd`) produce machine-readable reports under `data/acceptance/`. Automated regression: 296 passed, 1 environment-only DPAPI skip; Ruff, Black, mypy (66 files), and `pip check` clean. The managed runner detected the 1920x1080 display and controlled Notepad window, but PyAutoGUI's intentional corner fail-safe blocked live input; UAC/audio/multi-monitor gates therefore remain user-session checks. Final package: `dist/JARVIS/JARVIS.exe`, 18,414,990 bytes, SHA-256 `5A6908BABF7C1031B6214A633DBC761E000067D932B4CBEBC3CA64F3EC39C903`; 8-second smoke passed and all six plugin manifests are packaged. Checkpoint: `backups/tranche14_interactive_acceptance_20260818.zip` (secrets and runtime/build data excluded).

- AI OS agent tranche 12 (2026-08-18): complete typed desktop input skills, expanded broker system-management allowlist, DOM-assisted Trading Copilot, selective app close, pressure diagnosis, scoped memory release, venv creation, centralized LLM SDK gateway and architecture regression gates. Full regression: 292 passed with 1 environment-only DPAPI skip. Live UAC broker smoke could not establish its elevated pipe in the managed runner and remains manual.
- Static-quality expansion (2026-08-18): Black and Ruff cover the complete modern architecture; mypy expanded from 7 files to all 66 modern architecture/gateway files with zero errors. Full regression remains 292 passed with 1 environment-only DPAPI skip; `pip check` clean. Final rebuilt package: `dist/JARVIS/JARVIS.exe`, 18,414,478 bytes, SHA-256 `48991C68EE9CC40C4935A9537D92B779E4CC5A9D636CDED8F3F4D803C4928C23`; 8-second isolated smoke passed with all six plugin manifests and config packaged. Checkpoint: `backups/tranche13_full_static_quality_20260818.zip`.

- AI OS agent tranche 10 (2026-08-18): added bounded loopback CDP fallback, expanded Companion modes/detectors, recoverable worker watchdogs, process/GPU telemetry, hardware/audio/display events, Explain mode, shared errors and configured quality gates. Final regression: 280 passed with 1 environment-only DPAPI skip; Ruff, Black, scoped mypy and `pip check` clean. Rebuilt package smoke-tested for 8 seconds: `dist/JARVIS/JARVIS.exe`, SHA-256 `60F833FA2A056D0844DC131F1EFEB5C33171E052E172BE076C049EDFDFACD19B`. Checkpoint: `backups/final_quality_checkpoint_20260818.zip`. Live Notepad UIA remained environment-blocked because the sandbox had no interactive window handle.

- AI OS agent tranche 9 (2026-08-18): DPAPI CredentialVault, broker-controlled delayed LIMITED startup, 14-category Command Center, validated TerminalAgent, fully integrated transactional/archive FileAgent, multi-level secret-rejecting vector memory, UIA timeout/retry and skill audit history are implemented. Full regression: 268 passed with 1 environment-only skip; dependency integrity clean. Compliance evidence is tracked in `docs/REQUIREMENTS_MATRIX.md`.

- AI OS agent tranche 8 (2026-08-18): ProcessManager now exposes OS inventory/resource/hierarchy data and owned-process restart/tree termination. AppManager performs known/Start Menu/registry discovery, ambiguity-safe resolution and confirmed exact-ID Winget mutations through the broker. Context, network, clipboard, notifications, hardware inventory/change events and rotating secret-redacted audit logs are integrated. Required architecture/security/permissions/tools/development/performance documents now exist. Full regression: 246 passed with 1 environment-only skip.

- AI OS agent tranche 7 (2026-08-18): declarative plugins validate skill existence, capability containment and risk monotonicity; six application manifests ship inside the EXE. Managed processes now report spontaneous exits. Failure injection covers process crash, broker offline, API timeout/fallback and TTS failure. Full regression: 231 passed with 1 environment-only skip; `pip check` clean. Package smoke test remained alive for 6 seconds. Checkpoint: `backups/plugin_failure_checkpoint_20260818.zip`; report: `docs/FINAL_AI_OS_REPORT.md`.

- AI OS agent tranche 6 (2026-08-18): local streaming STT emits partials and recognizes cancellation phrases immediately; Claude/Kimi now stream into the same sentence/TTS pipeline used by OpenAI; HUD consumes authoritative state/emergency/broker/partial-transcript data; first-token and first-audio latency are measured. Full regression: 223 tests passed with 1 environment-only skip; `pip check` clean.
- AI OS agent tranche 5 (2026-08-18): MissionEngine now performs permission checks, confirmation waits, preconditions, dry-run, verified fallbacks and compensating rollback. Tool manifests carry enforceable risk plus timeout/retry/verification metadata, with one-time staged confirmations. Full regression: 218 tests passed with 1 environment-only skip; `pip check` clean.
- AI OS agent tranche 4 (2026-08-18): Chrome bridge authentication is now per-installation rather than hardcoded; commands are bounded, expiring and correlated with results. Tab/download controls, adaptive perception and volatile region/window/monitor capture are implemented. Full regression: 210 tests passed with 1 environment-only skip; `pip check` clean.
- AI OS agent tranche 3 (2026-08-18): global emergency stop now coordinates async tasks, missions, managed processes, automation, TTS/input release and state; recovery is bounded and verifiable; core state transitions are typed; file indexing is incremental and secret-aware. Full regression: 203 tests passed with 1 environment-only skip; `pip check` clean.
- AI OS agent tranche 2 (2026-08-18): completed broker lifecycle/UAC entrypoint and watchdog integration; introduced typed Win32 Window Manager, structured Windows UI Agent, transactional File Agent with rollback, and capability-based OpenAI/Claude/Kimi work distribution. Full regression: 191 tests passed with 1 environment-only skip; `pip check` clean. Live elevated IPC and real multi-monitor/UI application checks remain manual gates.
- Safe Mode and broker boundary (2026-08-18): `--safe` is enforced centrally at permission and capability routing layers. A separate authenticated AF_PIPE broker now validates signed, expiring, replay-protected, risk-declared requests and exposes only allowlisted Windows operations; Winget install/upgrade fail closed when it is offline. Automated protocol tests include tampering, replay, expiry, missing confirmation and unknown actions. Full regression: 179 tests passed with 1 environment-only skip; dependency check clean.
- Permission-engine hardening (2026-08-18): added deterministic SAFE/SENSITIVE/ADMIN/DESTRUCTIVE/FORBIDDEN classification, restored confirmation gates, fail-closed forbidden actions, PBKDF2 PIN verification and an unforgeable in-process confirmation marker. Full regression: 173 tests passed with 1 environment-only skip; `pip check` clean; changed modules compile.

- Full regression after the startup, routing and stability work: 129 passed in 5.901 seconds.
- Dependency integrity: `pip check` reports no broken requirements.
- Regression fixed: HUD adapters without a Companion service now degrade to an explicit unavailable/passive snapshot.
- Every request remains on the `ASSISTENTE` surface; operational routing no longer changes the visible tab.
- Startup now presents the splash immediately and preloads core runtime, router, voice queue and the local Vosk model before exposing the HUD.
- Unsupported `reasoning.effort=none` is normalized for GPT-5 family models, preventing the reported HTTP 400 failure.
- Unhandled main-thread and worker-thread failures are persisted to `logs/crash.jsonl` for post-mortem diagnosis.
- Settings reads use a signature-validated in-memory cache; writes remain atomic and externally edited files invalidate the cache.
- Local biometric identity is implemented for face and speaker recognition: multi-sample enrollment, local descriptors, DPAPI encryption on Windows, bounded camera capture, configurable thresholds and fail-closed device handling. Webcam and audio input discovery pass on the target PC; personal enrollment remains an explicit user action.
- Continuous wake audio now uses a bounded recent-frame queue and thread-safe Vosk initialization, preventing unbounded latency growth under CPU pressure.
- Current application verification: 136 tests pass outside the managed sandbox; DPAPI round-trip also passes under the real Windows account. All 127 project Python files compile successfully.
- Research and informational prompts now bypass the operational PC router, removing its 25-second timeout from the conversational path. Web-enabled requests automatically promote unsupported `minimal` reasoning to `low`; regression coverage reproduces the original Italian prompt. Current suite: 137 tests.
- PowerPoint creation is now a native routed capability that writes validated `.pptx` files to the Desktop. Combined face-and-voice recognition, the common `volte` transcription error and pre-enrollment fail-fast behavior are covered. Current suite: 141 tests.
- Startup CEO login now combines the current Windows DPAPI identity with multi-frame face recognition, publishes authenticated session state, fails to guest/setup state without lockout, and supports an explicit retry command. Current suite: 143 tests.
- Atomic multi-modal profile provisioning now stores name, role, per-category permissions, face templates, voice templates and the fallback phrase. Failed face login activates a restricted guest session; `Jarvis, sono io` requires speaker matching before restoring the stored session permissions. Current Windows suite: 145 tests with DPAPI enabled and no skips.
- HUD redesign baseline: `backups/hud_redesign_pre_20260811_005335.zip`. The production `main.py -> hud.JarvisHUD` assistant surface now uses a procedural cinematic renderer rather than the former left-card/core/right-card dashboard. The default surface has a ten-layer core, sparse technical field, connector pulses, clipped floating clusters, live OpenAI/model state, real telemetry, foreground-window context modes, transient real camera frames/detections and decoded-TTS RMS animation. Two Windows-native visual render passes were inspected; temporary screenshots were deleted after review. Current managed suite: 149 tests passed, one environment-only DPAPI skip; `pip check` clean.
- Final HUD checkpoint: `backups/hud_redesign_final_20260811_0215.zip`. Production EXE launched from the Desktop shortcut, remained responsive, added zero crash-log bytes during validation, and used about 495 MB RAM. Final regression: 150 tests passed (one managed-environment DPAPI skip); dependency check clean.
- Reference HUD delivery checkpoint: `backups/reference_hud_checkpoint_20260811_0314.zip` (pre-change archive: `backups/reference_hud_pre_20260811.zip`). The production `main.py -> hud.JarvisHUD` path now renders the supplied 16:9 reference composition directly: continuous telemetry rail, high-density multi-ring spherical core, real camera/system/OpenAI/task/context clusters, lower voice/memory/response modules, persistent command bar and five orbital navigation controls. The legacy assistant layout remains disabled and is retained only as non-instantiated compatibility code.
- The startup path now presents a fullscreen local identity gate before constructing `JarvisHUD`. It subscribes to camera events before biometric verification, displays only real in-memory frames and real detector boxes/FPS, clears them when capture stops, distinguishes setup-required/authenticated/rejected/unavailable states and transitions into the operational HUD only after the identity operation has released camera and microphone resources. The stored fallback phrase is now matched against the actual offline transcript as well as the voiceprint.
- Reference-alignment QA is fully in memory and persists no webcam capture. The old visual baseline scored 43.11/100; the delivered vector HUD scores 78.42/100, with 80.75% regional similarity, 92.75% core-geometry similarity, 0.8095 visible-density ratio and 0.5345 bright-density ratio. The remaining score gap is primarily the deliberately blank inactive-camera area: no reference photograph or fabricated telemetry is shown in place of a live feed.
- Verification for this delivery: the full suite passed 160 tests with one environment-only DPAPI skip before the final rendering-cache optimization; the final affected HUD/startup/provider/identity selection passes 31 tests with one expected DPAPI sandbox skip, all modified Python files compile, and `pip check` reports no broken requirements. A repeated full-suite run after caching exceeded the timeout on a machine already saturated by the user's older JARVIS instance, without reporting an assertion failure. Core-only benchmark after caching measured about 21 paint cycles/s, 91.47 MB RSS and 31.6% process CPU; whole-HUD figures under the concurrent old executable were too contention-sensitive to claim a reliable 60 FPS result.
- Final package: `dist_reference_final/JARVIS/JARVIS.exe`, 18,262,034 bytes, SHA-256 `E168935B5C028E10A541A5BAAA45B21474C3C9044BF6F9710A697E6DAD848FB5`. The isolated EXE smoke test opened and closed without leaving a process. Desktop shortcut `JARVIS - Nuovo HUD.lnk` targets this exact package; the previous running executable and shortcut were not overwritten.
- Provider-routing checkpoint: `backups/provider_router_pre_20260818.zip`. Conversational requests now use a deterministic provider router: current-information requests remain on OpenAI for web search, coding defaults to Claude when configured, and long-context analysis defaults to Kimi K3 when configured. Manual provider selection overrides automatic routing; unconfigured or failed providers are skipped through bounded fallbacks. Operational computer tools remain on the existing OpenAI Responses path. Secrets are exclusively environment variables (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `MOONSHOT_API_KEY`) and the exposed example key was removed. Rust toolchain `cargo 1.97.1` is installed and ready; no native extension was added before profiling identifies a CPU-bound candidate. Full regression: 166 tests passed, 1 expected sandbox-only DPAPI skip; modified sources compile and `pip check` is clean.
- Minimal-HUD checkpoint: `backups/hud_minimal_pre_20260818_1852.zip`. Production now exposes exactly one Home surface plus one Registro surface. Home contains the animated JARVIS circle and sole Log affordance; it has no text input, status copy, transcript, cards or telemetry. Voice questions, assistant answers and bounded runtime events are confined to Registro. Windows fonts are loaded explicitly so Qt offscreen and packaged rendering retain legible typography. Visual renders: `artifacts/hud_minimal_implementation_v2.png` and `artifacts/hud_minimal_log.png`. Verification: 169 tests passed, 1 expected sandbox-only DPAPI skip; `pip check` clean.
- Executable delivery checkpoint: `backups/exe_pre_hud_minimal_20260818.zip`. Rebuilt package is `dist_reference_final/JARVIS/JARVIS.exe` (18,305,750 bytes, built 2026-08-18 19:08). Desktop shortcut `C:\Users\gabri\Desktop\JARVIS.lnk` now targets this package; isolated smoke test stayed alive for 6 seconds without early exit.
- Startup/HUD refinement delivery: `main.py` now uses the same minimal animated-circle language during biometric/preload startup, including the restrained lower status rail. The Home keeps its approved upper/outer composition and adds only `VOCE`, `SESSIONE LOCALE` and `PRIVACY ATTIVA` at the bottom. Updated package: `dist_reference_final/JARVIS/JARVIS.exe` (18,310,248 bytes, built 2026-08-18 19:22). Focused HUD/pre-startup verification: 10 tests passed; isolated EXE smoke test stayed alive for 6 seconds; Desktop shortcut target verified.
- Identity-gate removal delivery: automatic CEO profile access, startup webcam subscription and biometric pre-check were removed from `main()`; startup now performs essential service preload only and opens the operational HUD directly. Explicit identity capabilities remain opt-in. Checkpoint: `backups/startup_identity_pre_remove_20260818.zip`. Updated package: `dist_reference_final/JARVIS/JARVIS.exe` (18,309,395 bytes, built 2026-08-18 19:33); Desktop target verified. Focused verification: 20 tests passed. Final EXE smoke launch was blocked by Windows application-control policy in this environment, so runtime launch remains environment-blocked.

Status follows executable code and tests, not this document when they disagree.

- Phase 3 — Advanced Working Memory + volatile World Model (2026-09-03): extended the existing volatile `WorkingMemory` backwards-compatibly with a 512-entry bound, lazy expiry cleanup, deterministic oldest-entry eviction, `delete`, `clear_prefix`, `namespace`, `contains`, `inspect` and `stats`; values remain defensive copies and no cleanup thread was added. Added canonical `jarvis_core.world_model.WorldModel`, backed only by `CORE_RUNTIME.memory.working` under `world.entities.*`; no world database, persistent memory, planner, EventBus or Cognitive Core was created. Beliefs are property-level and carry confidence, source, evidence type, monotonic freshness/TTL and update time. Supported evidence is `mentioned`, `intended`, `inferred`, `observed_structured`, `observed_perception` and `verified`; mentions never assert runtime state. Entity views cover application, window, artifact, UI target and task/mission. Deterministic contradiction policy lets newer structured process/runtime observations correct older verified tool state, while low-confidence vision cannot replace fresh structured evidence; conflicts are bounded to `last_conflict`. TTLs: UI 10s, windows 20s, applications 45s, artifacts 600s and current missions 300s. `CORE_RUNTIME.world` is created once after the existing MemoryStore, attached to the canonical ContextEngine, operational results and existing PerceptionEngine snapshot; refresh never calls an observer. Context snapshots expose bounded world data, AI compact context includes a small world projection, and mission state is read from the existing MissionStore. Secrets, raw screenshots, audio, complete file content and persistent world state are excluded. Added `tests/test_memory_advanced.py`, `tests/test_world_model.py` and `tests/manual_phase3_world_model.py`. Focused Phase 3/core regression: 123/123; final full regression: 577/577. Compileall, Ruff and `pip check` clean. Manual/hardware checklist was not executed. Baseline: `backups/phase3-world-model-baseline_20260903.zip` (SHA-256 `37B01999EED2046E48ABB50BC99D457CE12FB44A89CBC887F0D6F9A43556B5C1`). Final checkpoint: `backups/phase3-world-model-final_20260903.zip`.
- Phase 3 World Model integration fix (2026-09-03): `opened_apps` remains the backwards-compatible managed-process view; the WorldModel now uses the separate lightweight OS process inventory and visible-window observations, so absence from managed processes/windows does not falsely close normal applications. Application identities converge by executable basename/casefold and bounded observed aliases, including user-facing name plus executable. Focus is exclusive for accepted authoritative observations; low-confidence vision cannot displace fresh DOM/UIA/runtime focus. Identical beliefs and duplicate conflicts no longer emit repeated events. Verification reuses the canonical Phase 2 policy, property count/alias/entity/UI bounds are enforced, and WorkingMemory outer TTL covers the longest still-fresh property. Context/operational/perception integration regressions were added. Focused integration tests: 57/57; final full regression: 582/582. Manual/hardware tests remain unexecuted. Baseline: `backups/phase3-world-model-integration-fix-baseline_20260903.zip` (SHA-256 `024F4FF52EFDF6816DD0DE8882243BD22660F7BDCF6C7C289FED852264AF92B6`). Final checkpoint: `backups/phase3-world-model-integration-fix-final_20260903.zip`.
- PHASE 3 FINAL EDGE FIX (2026-09-03): `running_inventory_snapshot()` mantiene l'inventory bounded e comunica `complete`; il contesto espone `os_processes_complete`, e le assenze possono diventare `running=False` solo con inventory disponibile e completa. Inventory parziali forniscono solo evidenza positiva. Le osservazioni con lo stesso valore rinnovano silenziosamente `updated_at`/TTL e migliorano source, confidence ed evidence senza `world.updated`; l'outer TTL resta calcolato sulla property fresca più lontana. I conflict usano un fingerprint volatile per entità, recuperabile anche senza property accettate, evitando eventi duplicati per la stessa contraddizione. Aggiunti regression test inventory, freshness e conflict dedupe. Baseline: `backups/phase3-world-model-final-edgefix-baseline_20260903.zip` (SHA-256 `98E0C9ABD33617781F6861356FCFE68E5581D0D8C1D9FDE836CD195337CEF1F8`). Verifica finale: 587/587, compileall, Ruff e `pip check` passati; test manuali/hardware non eseguiti. Final checkpoint: `backups/phase3-world-model-final-edgefix-final_20260903.zip`.
- PHASE 3 CONFLICT TTL FINAL FIX (2026-09-03): aggiunto `CONFLICT_TTL = 45s` per il fingerprint volatile `last_conflict`, mantenuto nello stesso record `CORE_RUNTIME.memory.working`; l'outer TTL dell'entity copre anche la scadenza del conflict. Il fingerprint viene eliminato lazy alla scadenza, quindi lo stesso conflitto a t=0/5/20 produce un solo evento, mentre una ricomparsa dopo 45s può produrne uno nuovo. Regression fake-clock aggiunta. Verifica: test mirati 47/47, regressione completa 588/588, compileall/Ruff/`pip check` passati; test manuali/hardware non eseguiti. Baseline: `backups/phase3-conflict-ttl-finalfix-baseline_20260903.zip` (SHA-256 `DF4CEA5D4B30FCB998F07FC74C7E6BC5E847F153AE46B27894A688C01A1445FA`). Final checkpoint: `backups/phase3-conflict-ttl-finalfix-final_20260903.zip`.

- Phase 2 integration fix (2026-09-03): corrected only the existing conversation-context integration. User mentions remain volatile references and never become active objects; active objects are created only from real verified tool results or explicitly observed runtime state, and verified close actions invalidate the matching active object. The legacy context facade no longer fabricates verification. Tool recording now receives tool name, arguments and real result; local spoken/displayed responses are recorded through the canonical redacted working memory. Pending assistant proposals resolve before generic focus, are bounded/consumable, and explicit cancellation invalidates them. Main keeps control/confirmation precedence and passes conversational context internally without changing displayed user text. Generic artifact precedence is limited to explicit artifact follow-ups. Baseline checkpoint: `backups/phase2-integration-fix-baseline_20260903.zip` (SHA-256 `6701BD6D4233274538219DD850ED2AA388143548922C5A19076B0D6AEFC59BE4`).
- Phase 2 integration fix verification: focused regression 55/55; full unittest regression 559/559; compileall, Ruff and `pip check` clean. Hardware/microphone tests were not claimed as manual acceptance. Final checkpoint: `backups/phase2-integration-fix-final_20260903.zip`.
- Phase 2 reference-routing final integration fix (2026-09-03): the existing resolver result now feeds a dedicated internal operational text after resolution. `processa_domanda()` preserves the original user text for transcription/UI while routing `Chiudilo` as `chiudi Chrome` and `Aprilo` as `apri Spotify`; ambiguity still stops routing, verified artifact follow-up remains deterministic, pending proposals and sensitive confirmations retain precedence. Focused routing/Phase 2 tests pass 50/50; full regression passes 563/563. No Phase 3 work started. Baseline checkpoint: `backups/phase2-reference-routing-finalfix-baseline_20260903.zip` (SHA-256 `F63E2BE6E6285EA33CD0EDE91A15A8D4FC4D8F1FD7D935AF0E587D2280BF8C1C`). Final checkpoint: `backups/phase2-reference-routing-finalfix-final_20260903.zip`.

- Watchdog filesystem integration checkpoint (2026-09-02): baseline `backups/watchdog_pre_20260902_targeted_v2.zip` (SHA-256 `9FF5F1F585E25E1C16D312C70686189DC924E25E2BE7E2358C8E0B5A74A3DF6E`). Reused the existing core Watchdog/runtime, EventBus, NotificationCenter and SkillRegistry. Added owned in-process filesystem watcher registry plus native `watchdog.start`, `watchdog.stop` and `watchdog.list`; natural Italian routing now extracts Windows paths and created/modified/deleted/moved events without sending the request to conversational AI. Full regression: 523 tests passed; real E2E on `C:\Users\gabri\Desktop\JARVIS_WATCHDOG_TEST` passed with external create/modify, list, stop and clean shutdown. `watchdog` 6.0.0 is installed in `.runtime-env`; `pip check` is clean.
- Final Watchdog checkpoint: `backups/watchdog_final_20260902.zip`, SHA-256 `AC0475EA6234679E1BD03126B0335429C51F51868E54F830D55FEBE028FDCF2A`.

- Italian STT/entity repair checkpoint (2026-09-02): baseline `backups/stt_italian_pre_20260902_v2.zip` (SHA-256 `CBC68BD16E8C0577AF037E4F05E4721606BD3AF9B901FC6C276BE0817C2E90FD`). Added explicit configurable `stt_language=it` and `stt_language_lock=true`, bounded registry-derived OpenAI transcription context, and centralized context-aware post-STT entity repair before local routing. Vosk remains on the existing `model-it` path; wakeword, Silero VAD, TTS and barge-in were not changed. Deterministic fuzzy/phonetic matching resolves application/site entities only, preserves raw and normalized text, logs reason/confidence, asks clarification on close candidates, and leaves URLs, filenames, code and unknown entities untouched. Focused repair/STT/streaming/wakeword regressions and the full suite passed; final checkpoint and hash are recorded below. Real spoken microphone scenarios remain an explicit manual gate and were not claimed as passed.
- Final Italian STT/entity repair checkpoint: `backups/stt_italian_final_20260902.zip`, SHA-256 `181F0670D651145FB15A14C79316EEC95557B5F67EBEE8D18C42658288BF322`. Verification: 518 full unittest tests passed, focused repair/STT tests passed, `pip check` clean, audio configuration check passed with input device 3 detected. Real microphone speech validation remains pending manual interaction.
- Autonomous orchestration and response presentation (2026-09-02): extended the existing `SkillManifest`/`SkillRegistry` with planner metadata for all 139 registered skills; added the bounded `AutonomousOrchestrator` trace over the existing planner, router, executor, critic and verification state; mission-mode prompts now receive the registry capability catalog and failed steps generate retry/fallback guidance. Added the central `ResponseRenderer` producing `spoken_response` and `display_response`; normal speech hides URLs, full paths, tool names, IDs and technical errors, while the HUD keeps details and opens links through `QTextBrowser`. Added persistent technical mode commands. Focused autonomy/presentation tests passed, full regression passed with 529 tests, Ruff and compile checks passed, and `pip check` is clean. Runtime smoke passed with real registry Watchdog start/stop, verified orchestration trace and URL-safe spoken/display separation. Baseline checkpoint: `backups/autonomy_response_pre_20260902.zip`, SHA-256 `AE9C129E283BAB5C9AA350472D4A354D7975DFB6CA5A90D9620D230E4724844A`. Final checkpoint/hash recorded below.
- Final autonomy/response checkpoint: `backups/autonomy_response_final_20260902.zip`, SHA-256 `29D93475377B0DE534490DFDB269CCF2D48352D858AFEB3D64C581278023B831`.
- Lifecycle checkpoint (2026-09-01): baseline `backups/lifecycle_screenpipe_llama_pre_20260901_183713.zip` (SHA-256 `9A6E1DCD1E5AEC65099BBC9DA8E1AEF89C0D4F7A990FDFE8B52569641DAC0F4E`). Added centralized, ownership-aware Screenpipe/llama.cpp startup through `ProcessManager`; Screenpipe auth probes use Windows Keyring and llama readiness uses `/v1/models`. Ollama lifecycle was not changed. Real sidecar E2E passed; final checkpoint is recorded below.
- OpenHands lifecycle checkpoint (2026-09-02): baseline `backups/openhands_lifecycle_pre_20260902_152322.zip`. Added OpenHands to the existing `LocalServicesManager` with persistent Docker container `openhands-jarvis`, WSL2/Docker Desktop invocation, dynamic Ubuntu HOME resolution for `~/.openhands`, HTTP readiness on loopback 3000, bounded startup, and session ownership-aware shutdown. Existing Screenpipe, llama.cpp, Ollama and SearXNG paths were not changed. Focused lifecycle tests, Ruff and compileall passed. Real E2E passed for JARVIS-owned startup/shutdown and for an already-running external container. Final checkpoint: `backups/openhands_lifecycle_final_20260902.zip` (SHA-256 `3C54C707D9D475AA9EE8D6BDAF122DB2555F2221D87090055A571A29886CBA65`).
- EXE refresh (2026-08-19): rebuilt `dist/JARVIS/JARVIS.exe` with the football analysis capability and updated `C:\Users\gabri\Desktop\JARVIS.lnk` to the rebuilt executable. SHA-256 `FDFCCCFA1A3FAFA55E5D553C8215F1432D4BA657B01425D8F17A86FFE821A2BE`; eight-second smoke remained alive. Checkpoint: `backups/pre_exe_update_20260819_200332.zip`.
- Windows-control routing fix (2026-08-19): common compound commands such as `Apri Chrome e vai su TradingView` and `Apri Blocco Note e scrivi ...` are now split and executed locally in order, avoiding malformed AI arguments. Windows Settings variants route locally, and application opening safely discovers unlisted installed programs through unique Start-menu shortcuts. Live verification started Chrome and Notepad and completed the TradingView compound command. Full regression: 323 tests passed; `pip check` clean.
- Windows-control EXE delivery (2026-08-19): rebuilt `dist/JARVIS/JARVIS.exe` (18,545,390 bytes, SHA-256 `07319B39D5BB08F95AC9F434C80B3559A6ABD017A467DFAE4BCC3F5782D3D0A6`) and updated `C:\Users\gabri\Desktop\JARVIS.lnk`. Twelve-second packaged smoke remained alive and responsive with zero added crash-log bytes.
- Operational-capability routing hardening (2026-08-19): capability questions and wording around direct PC, mouse, keyboard, webcam, microphone, files and applications can no longer fall through to the generic chatbot disclaimer. Both conversational and operational prompts now describe the real local integration accurately; additional imperative verbs route to the operational agent. Registry audit confirms all 108 implemented functions have matching router schemas. Live probes verified mouse movement, keyboard input, file create/read/delete/restore, and staged shutdown confirmation. Full regression: 325 tests passed.
- Operational-capability EXE delivery (2026-08-19): rebuilt `dist/JARVIS/JARVIS.exe` (18,546,676 bytes, SHA-256 `9508FF52BD32499B98FD944CDF36D64BF6D6ACB5636AF215298574125B1FF824`) and refreshed `C:\Users\gabri\Desktop\JARVIS.lnk`. Packaged twelve-second smoke remained alive and responsive with zero new crash-log bytes; dependency check clean.
- Hardware/browser completion audit (2026-08-19): live volatile probes passed for microphone (one-second sample, metrics only, buffer zeroed) and webcam (640x480 single frame, zeroed, not persisted). Chrome launched by JARVIS now uses an isolated local profile with the bundled extension and loopback-only CDP on `127.0.0.1:9222`; CDP provides verified tab discovery/navigation when the extension bridge is unavailable. Live verification read Google, opened TradingView, verified the `AAPL` page and completed the advisory visual chart analysis without placing orders.
- Browser-control EXE delivery (2026-08-19): rebuilt `dist/JARVIS/JARVIS.exe` (18,548,456 bytes, SHA-256 `9DDEA56A9E154D9BCA7816EE114674857EBCD7F87AF33651EC96A2AAABB8CC32`) and refreshed the Desktop shortcut. Full regression and `pip check` passed; packaged twelve-second smoke remained alive and responsive with zero new crash-log bytes.
- Fullscreen night keyboard (2026-08-19): the production minimal HUD now toggles a clickable on-screen keyboard with the physical Space key only while fullscreen. It includes alphanumeric keys, backspace, space, clear, submit and close; submit emits the existing shared command signal. Escape closes the keyboard first, and minimizing always hides it. Focused HUD regression passes.
- Fullscreen night-keyboard EXE delivery (2026-08-19): rebuilt `dist/JARVIS/JARVIS.exe` (18,550,912 bytes, SHA-256 `27CA80A18121AB26249C69E49027077527FDECDAA2C0FE2D29AE1B3972E1B792`) and refreshed `C:\Users\gabri\Desktop\JARVIS.lnk`. Full regression and dependency checks passed; packaged twelve-second smoke remained alive and responsive with zero new crash-log bytes.
- Compact night keyboards (2026-08-19): the fullscreen Space-key keyboard is reduced to at most 760x350. A separate 620x320 always-on-top keyboard can be toggled beside the compact orb through the global Windows `Ctrl+M` hotkey only while JARVIS is minimized; it submits through the same command signal and remains independent from restoring the HUD. Focused HUD and callback regressions pass.
- Compact-keyboard EXE delivery (2026-08-19): rebuilt `dist/JARVIS/JARVIS.exe` (18,551,881 bytes, SHA-256 `9C98DF443777A0D08B1A67EBDE31DD26B4A2E173F9981AE3BB257FE232274088`) and refreshed the Desktop shortcut. Full regression and dependency checks passed; packaged smoke including real `Esc` then global `Ctrl+M` stayed alive and responsive with zero new crash-log bytes.
- Reasoning compatibility fix (2026-08-19): `gpt-5.6-luna` now receives `none` instead of unsupported `minimal`; web-search requests normalize both `none` and `minimal` to `low`. The router also retries once without optional reasoning if a provider rejects the effort field, preventing a model-vocabulary mismatch from aborting an otherwise valid command. Full regression: 329 tests passed; `pip check` clean. Packaged smoke remained alive and responsive; Desktop shortcut refreshed. EXE SHA-256 `3D806FA50EBE7BF09A864899ED70BBECE1CA71394AE1F99424071CDB7A3B45F9`.
